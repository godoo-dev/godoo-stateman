---
phase: 01-bootstrap-schema-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/godoo_stateman/__init__.py
  - src/godoo_stateman/errors.py
  - src/godoo_stateman/cli/__init__.py
  - src/godoo_stateman/cli/app.py
  - src/godoo_stateman/cli/commands/__init__.py
  - src/godoo_stateman/cli/commands/plan.py
  - src/godoo_stateman/cli/commands/apply.py
  - src/godoo_stateman/cli/commands/verify.py
  - src/godoo_stateman/cli/commands/import_.py
  - src/godoo_stateman/cli/commands/snapshot.py
  - tests/__init__.py
  - tests/unit/__init__.py
  - tests/unit/test_cli_help.py
autonomous: true
requirements:
  - UX-06
  - PKG-04
  - PKG-05

must_haves:
  truths:
    - "`uv run godoo-stateman --help` exits 0 and the output contains plan, apply, verify, import, snapshot"
    - "Stubbed commands (plan, apply, verify, import) print a Rich message and exit with code 1"
    - "`snapshot` command is registered and shows its docstring in --help"
    - "pyproject.toml uses hatchling build backend with `requires-python = >=3.14`"
    - "Error hierarchy: `StatemanError` and `VersionMismatchError` exist in `errors.py`; neither subclasses OdooError"
    - "GitHub repo `godoo-dev/godoo-stateman` is public; CLAUDE.md first line is the @-import of ../godoo-hq/UMBRELLA_CLAUDE.md"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, hatchling build backend, all runtime deps, tool config (ruff/mypy/pytest)"
      contains: "hatchling"
    - path: "src/godoo_stateman/errors.py"
      provides: "StatemanError, VersionMismatchError"
      exports: ["StatemanError", "VersionMismatchError"]
    - path: "src/godoo_stateman/cli/app.py"
      provides: "Typer app object with 5 registered commands"
      exports: ["app"]
    - path: "tests/unit/test_cli_help.py"
      provides: "Unit test: --help exits 0; all 5 command names listed; stubs exit 1"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "src/godoo_stateman/cli/app.py::app"
      via: "hatchling entry point"
      pattern: "godoo-stateman.*cli\\.app:app"
    - from: "src/godoo_stateman/cli/app.py"
      to: "src/godoo_stateman/cli/commands/*.py"
      via: "app.add_typer() or @app.command()"
      pattern: "app\\.command|add_typer"
---

## Phase Goal

**As a** developer evaluating godoo-stateman, **I want to** run `godoo-stateman --help` and see five commands listed, **so that** I can confirm the project is correctly installed and structured before configuring any Odoo connection.

<objective>
Scaffold the complete godoo-stateman Python project: pyproject.toml (hatchling + uv workspace + all deps), src layout, error hierarchy, and a Typer CLI with all five commands registered. Four commands are stubs; `snapshot` is registered with a real docstring. Confirm the public GitHub repository exists (PKG-05). The walking skeleton proves the entry point works end-to-end.

Purpose: Everything in Phase 1 depends on the package structure and the CLI entry point existing. Without this scaffold, no other plan can run.
Output: A `uv run godoo-stateman --help` that exits 0 listing plan, apply, verify, import, snapshot; a pyproject.toml that `uv build` can process; an error hierarchy that downstream plans extend; a confirmed-public GitHub repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-schema-registry/01-CONTEXT.md
@.planning/phases/01-bootstrap-schema-registry/01-RESEARCH.md
@.planning/phases/01-bootstrap-schema-registry/01-PATTERNS.md

<interfaces>
<!-- Key contracts this plan establishes for downstream plans. -->

From src/godoo_stateman/errors.py (to be created — downstream plans depend on this):
```python
class StatemanError(Exception):
    """Base class for all godoo-stateman local errors."""

class VersionMismatchError(StatemanError):
    """Raised when a snapshot odoo_version or schema_format_version mismatches."""
```

From src/godoo_stateman/cli/app.py (to be created):
```python
import typer
app = typer.Typer(name="godoo-stateman", help="Terraform for Odoo — declarative Odoo state reconciliation")
# Commands: plan, apply, verify, import_, snapshot registered via @app.command()
```

Stub command contract (plan/apply/verify/import_):
```python
def plan(...):
    console = Console()
    console.print("[yellow]plan: not yet implemented (Phase 3)[/yellow]")
    raise typer.Exit(code=1)
```

Async bridge contract for snapshot (structural — impl comes in plan 02):
```python
import asyncio

def snapshot(...):
    asyncio.run(_snapshot_impl(...))

async def _snapshot_impl(...) -> None:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pyproject.toml + package skeleton</name>
  <files>
    pyproject.toml,
    src/godoo_stateman/__init__.py,
    src/godoo_stateman/errors.py
  </files>
  <read_first>
    .planning/phases/01-bootstrap-schema-registry/01-PATTERNS.md (pyproject.toml analog at lines 39-101; errors.py analog at lines 105-158),
    .planning/phases/01-bootstrap-schema-registry/01-RESEARCH.md (pyproject.toml scaffold at lines 592-649; Package Legitimacy Audit at lines 141-156)
  </read_first>
  <behavior>
    - `StatemanError` is a subclass of `Exception`, not of any godoo-py error class
    - `VersionMismatchError` is a subclass of `StatemanError`
    - Both errors accept a string message and pass it to `super().__init__(message)`
    - `pyproject.toml` has `requires-python = ">=3.14"`, `license = "LGPL-3.0-or-later"`, hatchling build backend
    - `pyproject.toml` has `[project.scripts]` entry: `godoo-stateman = "godoo_stateman.cli.app:app"`
    - `[tool.uv.sources]` maps godoo-client, godoo-introspection, godoo-testcontainers to `path = "../godoo-py", editable = true`
    - `[tool.pytest.ini_options]` includes `asyncio_mode = "auto"`, `asyncio_default_fixture_loop_scope = "session"`, `testpaths = ["tests"]`, `markers = ["integration: ..."]`, `addopts = "--tb=short -q"`
    - All Python files begin with `from __future__ import annotations`
  </behavior>
  <action>
    Create pyproject.toml using the exact structure from PATTERNS.md pyproject.toml analog (lines 39-101) combined with RESEARCH.md scaffold (lines 592-649). Key values: name = "godoo-stateman", version = "0.1.0", description = "Terraform for Odoo — declarative Odoo state reconciliation over jsonrpc", license = "LGPL-3.0-or-later", requires-python = ">=3.14". Runtime deps: godoo-client>=0.2.0, godoo-introspection>=0.2.0, typer>=0.25.1, pydantic>=2.13.4, rich>=15.0.0, platformdirs>=4.9.6. Dev deps: pytest>=8, pytest-asyncio>=0.24, ruff>=0.8, mypy>=1.13, godoo-testcontainers>=0.2.0. Include all tool sections (ruff, mypy, pytest). Entry point: `godoo-stateman = "godoo_stateman.cli.app:app"`.

    Create src/godoo_stateman/__init__.py as a minimal package init with `from __future__ import annotations` header only. No exports at this level.

    Create src/godoo_stateman/errors.py following PATTERNS.md errors.py analog (lines 105-158). Two classes: `StatemanError(Exception)` — base for all local stateman errors; `VersionMismatchError(StatemanError)` — raised when snapshot version fields mismatch. Neither subclasses godoo-py OdooError. Include module docstring: "godoo-stateman local error hierarchy." Do NOT port godoo-py's OdooError hierarchy into this file — those are available by importing from godoo.client.errors directly.
  </action>
  <verify>
    <automated>cd /c/dev/godoo-dev/godoo-stateman && uv run python -c "from godoo_stateman.errors import StatemanError, VersionMismatchError; e = VersionMismatchError('test'); assert isinstance(e, StatemanError); assert isinstance(e, Exception); print('errors OK')"</automated>
  </verify>
  <done>
    `pyproject.toml` exists with hatchling backend, correct deps, entry point, and all tool sections. `src/godoo_stateman/errors.py` exports `StatemanError` and `VersionMismatchError`. Import check exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typer CLI skeleton + unit test</name>
  <files>
    src/godoo_stateman/cli/__init__.py,
    src/godoo_stateman/cli/app.py,
    src/godoo_stateman/cli/commands/__init__.py,
    src/godoo_stateman/cli/commands/plan.py,
    src/godoo_stateman/cli/commands/apply.py,
    src/godoo_stateman/cli/commands/verify.py,
    src/godoo_stateman/cli/commands/import_.py,
    src/godoo_stateman/cli/commands/snapshot.py,
    tests/__init__.py,
    tests/unit/__init__.py,
    tests/unit/test_cli_help.py
  </files>
  <read_first>
    .planning/phases/01-bootstrap-schema-registry/01-RESEARCH.md (Pattern 1: Typer async bridge at lines 249-268; stub pattern at lines 468-475; D-16 through D-19 at lines 36-42),
    .planning/phases/01-bootstrap-schema-registry/01-PATTERNS.md (cli/app.py analog at lines 410-433; commands/snapshot.py analog at lines 436-476; test_cli_help.py analog at lines 561-588)
  </read_first>
  <behavior>
    - `uv run godoo-stateman --help` exits 0 and output contains all five of: plan, apply, verify, import, snapshot
    - `uv run godoo-stateman plan` prints a Rich yellow message mentioning "Phase 3" and exits with code 1 (not 0, not 2)
    - `uv run godoo-stateman apply` prints a Rich yellow message mentioning "Phase 4" and exits with code 1
    - `uv run godoo-stateman verify` prints a Rich yellow message mentioning "Phase 5" and exits with code 1
    - `uv run godoo-stateman import` prints a Rich yellow message mentioning "Phase 3" and exits with code 1
    - `uv run godoo-stateman snapshot --help` exits 0 and shows argument help (config file path)
    - `app` is a `typer.Typer` instance exported from `cli/app.py`
    - No `async def` function is decorated with `@app.command()` — all command functions are synchronous wrappers
    - `snapshot` command body calls `asyncio.run(_snapshot_impl(config))` — `_snapshot_impl` is a stub `async def` that prints "snapshot: wiring in progress" for now (plan 02 replaces it)
  </behavior>
  <action>
    Create src/godoo_stateman/cli/app.py: instantiate `app = typer.Typer(name="godoo-stateman", help="Terraform for Odoo — declarative Odoo state reconciliation over jsonrpc.", add_completion=False)`. Register commands by importing each command function from `godoo_stateman.cli.commands.*` and decorating with `@app.command()`. The `import` command lives in `import_.py` and must be registered as `@app.command("import")` (since `import` is a Python keyword, the module is `import_.py` but the CLI name is `import`).

    Create src/godoo_stateman/cli/commands/plan.py, apply.py, verify.py, import_.py as stubs following D-18: each is a `def <name>(...)` with a `rich.console.Console()` that prints `"[yellow]<name>: not yet implemented (Phase N)[/yellow]"` then `raise typer.Exit(code=1)`. Phase mapping: plan → Phase 3, apply → Phase 4, verify → Phase 5, import → Phase 3.

    Create src/godoo_stateman/cli/commands/snapshot.py: a synchronous `def snapshot(config: Path = typer.Argument(..., help="Path to stateman config .py file"))` that calls `asyncio.run(_snapshot_impl(config))`. The `async def _snapshot_impl(config: Path) -> None` is a stub that prints `"[cyan]snapshot: schema registry wiring in progress[/cyan]"` via Rich Console (plan 02 replaces this body entirely).

    Create tests/unit/test_cli_help.py covering UX-06: use `typer.testing.CliRunner` (from typer[testing], available via typer install) to invoke `app` with `["--help"]` and assert exit_code == 0 and all five command names appear in output. Add tests that invoke each stub command and assert exit_code == 1. Use top-level test functions (no test classes) — godoo-py convention per PATTERNS.md lines 519-529.
  </action>
  <verify>
    <automated>cd /c/dev/godoo-dev/godoo-stateman && uv run pytest tests/unit/test_cli_help.py -x -q</automated>
  </verify>
  <done>
    `uv run pytest tests/unit/test_cli_help.py -x -q` exits 0. `uv run godoo-stateman --help` exits 0 and output contains plan, apply, verify, import, snapshot. Each stub command exits with code 1.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm public GitHub repository (PKG-05)</name>
  <files>
    (no source files — repository metadata verification)
  </files>
  <read_first>
    .planning/phases/01-bootstrap-schema-registry/01-RESEARCH.md (PKG-05 row in Phase Requirements table at line 65; Validation Architecture PKG-05 row),
    CLAUDE.md (confirm first line is the @-import of ../godoo-hq/UMBRELLA_CLAUDE.md)
  </read_first>
  <action>
    Verify the GitHub repository `godoo-dev/godoo-stateman` exists and is public. Run `gh repo view godoo-dev/godoo-stateman --json visibility`. If the command succeeds and visibility is `PUBLIC`, the requirement half is satisfied — do nothing further. If the command fails with a not-found error (repo does not exist), create it as public: `gh repo create godoo-dev/godoo-stateman --public --source . --remote origin --description "Terraform for Odoo — declarative Odoo state reconciliation over jsonrpc"` (the `--source .` flag uses the current local repo; if a remote named origin already exists, instead run `gh repo create godoo-dev/godoo-stateman --public` and wire the remote manually). If the repo exists but is PRIVATE, make it public: `gh repo edit godoo-dev/godoo-stateman --visibility public --accept-visibility-change-consequences`.

    Do NOT modify CLAUDE.md — its `@`-import of `../godoo-hq/UMBRELLA_CLAUDE.md` is already present (source-verified in RESEARCH.md). This task covers only the repo-public half of PKG-05. Do NOT read any secret values (gh uses its own stored auth token; never echo it).
  </action>
  <verify>
    <automated>gh repo view godoo-dev/godoo-stateman --json visibility -q '.visibility' | grep -iq public && echo "PKG-05 repo public OK"</automated>
  </verify>
  <done>
    `gh repo view godoo-dev/godoo-stateman --json visibility` returns `PUBLIC`. CLAUDE.md @-import confirmed present (no edit needed). PKG-05 fully satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → CLI | Odoo credentials flow in from environment variables; never from CLI args or config files |
| CLI → godoo-py | OdooClient receives credentials from env; no other credential path exists in Phase 1 |
| local repo → GitHub | `gh` CLI uses its own stored auth token to create/inspect the public repo; token never read into context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | pyproject.toml / any config file | mitigate | Credentials sourced from env only (`ODOO_URL`, `ODOO_DB`, `ODOO_USER`, `ODOO_PASSWORD`); `pyproject.toml` contains no credentials — verify no secrets appear in project files |
| T-01-02 | Tampering | npm/pip/cargo installs | mitigate | All packages in Package Legitimacy Audit (RESEARCH.md lines 141-156); `platformdirs` is [ASSUMED] but well-established; no [SUS] or [SLOP] packages |
| T-01-03 | Information Disclosure | `gh` auth token (Task 3) | mitigate | `gh` reads its token from its own credential store; never read/echo the token; use only `gh repo view`/`gh repo create`/`gh repo edit` subcommands |
| T-01-SC | Tampering | uv add / pip install | mitigate | slopcheck audit performed in RESEARCH.md; `platformdirs` tagged [ASSUMED] — executor must verify `pypi.org/project/platformdirs` is the tox-dev/platformdirs package before `uv add` |

</threat_model>

<verification>
After all tasks:
- `uv run godoo-stateman --help` exits 0 and stdout contains: plan, apply, verify, import, snapshot
- `uv run pytest tests/unit/test_cli_help.py -x -q` exits 0
- `uv run python -c "from godoo_stateman.errors import StatemanError, VersionMismatchError"` exits 0
- `uv build` produces a `.whl` in `dist/`
- `gh repo view godoo-dev/godoo-stateman --json visibility` returns PUBLIC
</verification>

<success_criteria>
1. `uv run godoo-stateman --help` exits 0 listing all five commands (plan, apply, verify, import, snapshot) — per D-16.
2. Each stub command (plan/apply/verify/import) exits with code 1 and prints a Rich yellow message naming the target phase — per D-18.
3. `snapshot --help` exits 0 and shows the config argument — per D-17 (functional in plan 02; registered here).
4. `pyproject.toml` satisfies: hatchling build backend, `requires-python = ">=3.14"`, LGPL-3.0-or-later license, all runtime deps listed — per PKG-04.
5. `StatemanError` and `VersionMismatchError` are importable from `godoo_stateman.errors` — used by plan 02.
6. GitHub repo `godoo-dev/godoo-stateman` is public; CLAUDE.md @-import present — per PKG-05.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-schema-registry/01-01-SUMMARY.md` when done.
</output>
