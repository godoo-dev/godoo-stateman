---
phase: 02-dsl-eval-pure-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/godoo_stateman/errors.py
  - src/godoo_stateman/dsl/__init__.py
  - src/godoo_stateman/dsl/types/__init__.py
  - src/godoo_stateman/dsl/types/desired.py
  - src/godoo_stateman/dsl/types/nodes.py
  - src/godoo_stateman/dsl/types/deferred.py
  - tests/unit/dsl/__init__.py
  - tests/unit/dsl/test_deferred.py
autonomous: true
requirements:
  - CORE-01
  - RSRC-06
  - RSRC-07
  - REL-01

must_haves:
  truths:
    - "A Python config file's module declaration is capturable in a typed DesiredState container"
    - "resolve() returns a Deferred thunk and never calls fn"
    - "Deferred.deps contains exactly the slugs of the resource refs passed to resolve()"
    - "Deferred is frozen — mutation raises FrozenInstanceError"
    - "networkx is a runtime dependency (not dev-only)"
  artifacts:
    - path: "src/godoo_stateman/dsl/types/desired.py"
      provides: "DesiredState frozen Pydantic model"
      contains: "class DesiredState"
    - path: "src/godoo_stateman/dsl/types/nodes.py"
      provides: "ResourceNode, DataSourceNode, ChildrenWrapper frozen dataclasses"
      contains: "class ResourceNode"
    - path: "src/godoo_stateman/dsl/types/deferred.py"
      provides: "Deferred frozen dataclass and resolve() function"
      contains: "class Deferred"
    - path: "src/godoo_stateman/errors.py"
      provides: "CycleError, DslEvalError, MissingModuleError error subclasses"
      contains: "class CycleError"
    - path: "tests/unit/dsl/test_deferred.py"
      provides: "Unit tests for resolve() contract — RSRC-06, D-04/D-05, SC-5 prerequisites"
      contains: "def test_resolve_returns_deferred"
  key_links:
    - from: "src/godoo_stateman/dsl/types/nodes.py"
      to: "src/godoo_stateman/dsl/types/desired.py"
      via: "DesiredState.resources: tuple[ResourceNode, ...]"
      pattern: "tuple\\[ResourceNode"
    - from: "src/godoo_stateman/dsl/types/deferred.py"
      to: "src/godoo_stateman/dsl/types/nodes.py"
      via: "ResourceNode.fields: dict[str, Any] may hold Deferred values"
      pattern: "Deferred"
---

<objective>
Phase 2 foundation: add networkx as runtime dependency, extend errors.py with the three
Phase 2 error subclasses, define all frozen DSL type contracts (DesiredState, ResourceNode,
DataSourceNode, ChildrenWrapper, Deferred), and unit-test the resolve() contract.

Purpose: Every downstream Phase 2 plan imports from this foundation — dsl/context.py,
eval.py, normalize.py, and graph.py all import ResourceNode, DataSourceNode, DesiredState,
and Deferred. Getting the contracts right here prevents cascading rework.

Output: pyproject.toml with networkx runtime dep; errors.py extended with DslEvalError,
MissingModuleError, CycleError; dsl/types/ package with three modules; test_deferred.py
covering RSRC-06 and the D-04/D-05 contract.
</objective>

<execution_context>
@C:\Users\marc\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\marc\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dsl-eval-pure-pipeline/02-CONTEXT.md
@.planning/phases/02-dsl-eval-pure-pipeline/02-RESEARCH.md
@.planning/phases/02-dsl-eval-pure-pipeline/02-PATTERNS.md

<interfaces>
<!-- Phase 1 types this plan extends — read before writing. -->

From src/godoo_stateman/errors.py (EXTEND — do not replace):
```
class StatemanError(Exception)
class VersionMismatchError(StatemanError)
```

From src/godoo_stateman/types/schema.py (reference — frozen Pydantic pattern):
```
class VersionedFieldSchema(BaseModel):
    model_config = ConfigDict(frozen=True)
    ttype: str
    relation: str | None

class VersionedModelSchema(BaseModel):
    model_config = ConfigDict(frozen=True)
    fields: dict[str, VersionedFieldSchema]
```

From src/godoo_stateman/schema/snapshot.py (reference — VersionedSnapshot injected into normalize):
```
class VersionedSnapshot(BaseModel):
    model_config = ConfigDict(frozen=True)
    models: dict[str, VersionedModelSchema]
```

From pyproject.toml [project.dependencies] (extend — move networkx here):
```
dependencies = [
    "godoo-client>=0.2.0",
    ...
    "platformdirs>=4.9.6",
]
```
networkx>=3.6.1 is currently in [dependency-groups] dev — move it to [project.dependencies].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add networkx runtime dep + extend errors.py with Phase 2 subclasses</name>
  <files>pyproject.toml, src/godoo_stateman/errors.py</files>
  <read_first>
    - pyproject.toml — read [project.dependencies] block (lines 8-15) and [dependency-groups] dev block (lines 41-53) to confirm current networkx placement and existing dep list before editing
    - src/godoo_stateman/errors.py — read all 18 lines before appending new subclasses
  </read_first>
  <action>
    In pyproject.toml: move "networkx>=3.6.1" from [dependency-groups] dev to [project.dependencies]. The entry must appear as a runtime dep so that `import networkx` works for end-users installing from PyPI (RESEARCH.md A3). Remove it from dev group after adding to project deps.

    In src/godoo_stateman/errors.py: append three new error subclasses after the existing VersionMismatchError, preserving the exact `__init__(self, message: str) -> None` pattern from the existing file (PATTERNS.md errors.py section). Three classes to add:
    - `DslEvalError(StatemanError)` — raised when a DSL config file fails to evaluate
    - `MissingModuleError(DslEvalError)` — note: subclasses DslEvalError, NOT StatemanError directly; raised when the top-level `module = "..."` declaration is absent or not a non-empty str
    - `CycleError(StatemanError)` — subclasses StatemanError directly (not DslEvalError); raised by graph.py when a dependency cycle is found

    Do not modify any other file. Do not add imports. Do not change the existing VersionMismatchError.
  </action>
  <behavior>
    - Test: import CycleError, DslEvalError, MissingModuleError from godoo_stateman.errors — all three importable
    - Test: MissingModuleError("x").args[0] == "x" — __init__ passes message to super()
    - Test: isinstance(MissingModuleError("x"), DslEvalError) is True
    - Test: isinstance(DslEvalError("x"), StatemanError) is True
    - Test: isinstance(CycleError("x"), StatemanError) is True
  </behavior>
  <verify>
    <automated>cd C:\dev\godoo-dev\godoo-stateman && uv run python -c "from godoo_stateman.errors import CycleError, DslEvalError, MissingModuleError, StatemanError; assert isinstance(MissingModuleError('x'), DslEvalError); assert isinstance(DslEvalError('x'), StatemanError); assert isinstance(CycleError('x'), StatemanError); print('errors ok')"</automated>
  </verify>
  <done>errors.py contains CycleError, DslEvalError, MissingModuleError with correct inheritance; pyproject.toml has networkx>=3.6.1 in [project.dependencies] and removed from dev group</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create dsl/types/ package — DesiredState, ResourceNode, DataSourceNode, ChildrenWrapper, Deferred + unit tests</name>
  <files>
    src/godoo_stateman/dsl/__init__.py,
    src/godoo_stateman/dsl/types/__init__.py,
    src/godoo_stateman/dsl/types/desired.py,
    src/godoo_stateman/dsl/types/nodes.py,
    src/godoo_stateman/dsl/types/deferred.py,
    tests/unit/dsl/__init__.py,
    tests/unit/dsl/test_deferred.py
  </files>
  <read_first>
    - src/godoo_stateman/schema/snapshot.py — frozen Pydantic pattern (model_config = ConfigDict(frozen=True)) to replicate for DesiredState
    - src/godoo_stateman/schema/version.py — @dataclass(frozen=True) pattern to replicate for ResourceNode, DataSourceNode, Deferred
    - src/godoo_stateman/identity.py — from __future__ import annotations + TYPE_CHECKING guard pattern
    - src/godoo_stateman/errors.py — verify DslEvalError/CycleError/MissingModuleError exist after Task 1
    - tests/unit/test_schema_registry.py lines 1-47 — module docstring, imports, helper factory, top-level test function convention to replicate
  </read_first>
  <action>
    Create the following files. All src/ files must begin with `from __future__ import annotations`.

    src/godoo_stateman/dsl/__init__.py — empty package marker file with a module docstring: "godoo-stateman DSL evaluation layer."

    src/godoo_stateman/dsl/types/__init__.py — empty package marker with docstring: "DSL type contracts — frozen value objects for the pipeline."

    src/godoo_stateman/dsl/types/deferred.py — implements:
    - `@dataclass(frozen=True) class Deferred` with fields `fn: Any` (NOT `Callable[..., Any]` — PATTERNS.md note: Callable annotation causes Pydantic schema generation issues) and `deps: frozenset[str]`
    - `def resolve(fn: Any, *refs: Any) -> Deferred` — extracts `.slug` or `.node_key` from each ref (using `hasattr`) to build `frozenset[str]` deps; never calls `fn`; handles both ResourceNode (.slug) and DataSourceNode (.node_key) refs per RESEARCH.md Q3 resolution
    - Imports: `from __future__ import annotations`, `from dataclasses import dataclass`, `from typing import Any`

    src/godoo_stateman/dsl/types/nodes.py — implements four frozen dataclasses:
    - `ResourceNode` with fields: `model: str`, `slug: str`, `fields: dict[str, Any]` (note: dict is inherently mutable — document in docstring that this matches Phase-1 precedent), `xmlid_module: str | None = None`, `parent_slug: str | None = None`
    - `DataSourceNode` with fields: `model: str`, `selector: dict[str, Any]`, `node_key: str` — `node_key` is a deterministic string derived externally in format `f"data.{model}[{sorted_selector_repr}]"` (per CONTEXT.md D-03 and RESEARCH.md Q2 recommendation)
    - `ChildrenWrapper` with fields: `child_model: str`, `inverse_field: str`, `children: tuple[Any, ...]`
    - Imports: `from __future__ import annotations`, `from dataclasses import dataclass`, `from typing import Any`
    - Note: ChildrenWrapper.children is `tuple[Any, ...]` (not `tuple[ResourceNode, ...]`) to avoid import cycle since ResourceNode is in the same file — use `Any`

    src/godoo_stateman/dsl/types/desired.py — implements:
    - `class DesiredState(BaseModel)` with `model_config = ConfigDict(frozen=True, arbitrary_types_allowed=True)` — the `arbitrary_types_allowed=True` is MANDATORY because ResourceNode.fields may hold Deferred which has a callable `fn: Any` field (PATTERNS.md Pitfall 4 / RESEARCH.md Pitfall 4)
    - Fields: `module: str`, `resources: tuple[ResourceNode, ...]`, `data_sources: tuple[DataSourceNode, ...]`, `config_parameters: tuple[dict[str, str], ...]`
    - Use `tuple` not `list` for all collection fields (frozen=True freezes attribute binding not mutable containers)
    - Imports: `from __future__ import annotations`, `from pydantic import BaseModel, ConfigDict`, `from godoo_stateman.dsl.types.nodes import ResourceNode, DataSourceNode`

    tests/unit/dsl/__init__.py — empty package marker.

    tests/unit/dsl/test_deferred.py — module docstring: "Unit tests for Deferred and resolve() — RSRC-06, D-04/D-05 contract." Tests (all plain `def`, no `async def`, no class TestX):
    - `test_resolve_returns_deferred` — resolve(lambda x: x, some_resource_node) returns Deferred instance
    - `test_resolve_never_calls_fn` — fn callable that records calls; verify after resolve() call that fn was NOT called
    - `test_resolve_deps_from_resource_slug` — pass ResourceNode with slug="parent"; assert deps == frozenset({"parent"})
    - `test_resolve_deps_from_data_source_node_key` — pass DataSourceNode with node_key="data.res_users[login=admin]"; assert deps frozenset contains "data.res_users[login=admin]"
    - `test_resolve_empty_refs` — resolve(lambda: None) returns Deferred with deps == frozenset()
    - `test_deferred_is_frozen` — attempt to set deferred.fn = None, assert dataclasses.FrozenInstanceError raised
    - `test_deferred_deps_type` — assert isinstance(Deferred(fn=lambda: None, deps=frozenset()).deps, frozenset)
    Follow _make_* helper factory convention from test_schema_registry.py for building ResourceNode/DataSourceNode test instances.
  </action>
  <behavior>
    - Test: DesiredState(module="x", resources=(), data_sources=(), config_parameters=()) constructs without error
    - Test: Attempting to set desired_state.module = "y" raises pydantic ValidationError (frozen=True)
    - Test: ResourceNode(model="res.partner", slug="p1", fields={}) constructs without error
    - Test: DataSourceNode(model="res.users", selector={"login": "admin"}, node_key="data.res_users[login=admin]") constructs without error
    - Test: resolve() returns Deferred; fn is not called; deps matches slugs/node_keys of refs
    - Test: Deferred mutation raises FrozenInstanceError
  </behavior>
  <verify>
    <automated>cd C:\dev\godoo-dev\godoo-stateman && uv run pytest tests/unit/dsl/test_deferred.py -x -q</automated>
  </verify>
  <done>All 7 test_deferred.py tests pass; DesiredState, ResourceNode, DataSourceNode, ChildrenWrapper, Deferred importable from their respective modules; dsl/ and dsl/types/ packages exist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan author → type constructors | ResourceNode/DataSourceNode/DesiredState construction from DSL-eval output; all inputs are Python objects, not external data |
| pyproject.toml → pip install | networkx added as runtime dep; legitimacy verified in RESEARCH.md Package Legitimacy Audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Deferred.fn callable field | accept | DSL configs are author-supplied and version-controlled; fn is never called in Phase 2; restricted builtins enforced in eval.py (Plan 02) |
| T-02-02 | Information Disclosure | ResourceNode.fields dict (mutable) | accept | dict is not frozen by @dataclass(frozen=True); matches Phase-1 VersionedModelSchema.fields precedent; no PII in this phase; documented in docstring |
| T-02-SC | Tampering | networkx runtime dep install | mitigate | networkx audited in RESEARCH.md Package Legitimacy Audit as approved (NSF-funded, 18yr, >10M downloads/wk); no checkpoint needed |
</threat_model>

<verification>
After both tasks complete:
- `uv run pytest tests/unit/dsl/test_deferred.py -x -q` exits 0 (all 7 tests pass)
- `uv run python -c "from godoo_stateman.dsl.types.desired import DesiredState; from godoo_stateman.dsl.types.nodes import ResourceNode, DataSourceNode, ChildrenWrapper; from godoo_stateman.dsl.types.deferred import Deferred, resolve; print('all imports ok')"` exits 0
- `uv run python -c "import networkx; print(networkx.__version__)"` exits 0 (verifies runtime dep)
- `uv run python -c "from godoo_stateman.errors import CycleError, DslEvalError, MissingModuleError; print('errors ok')"` exits 0
</verification>

<success_criteria>
1. networkx>=3.6.1 is in [project.dependencies] (not only in dev deps).
2. errors.py exports CycleError, DslEvalError, MissingModuleError with correct inheritance chain.
3. dsl/types/ package exports DesiredState (frozen Pydantic, arbitrary_types_allowed=True), ResourceNode, DataSourceNode, ChildrenWrapper (frozen dataclasses), Deferred (frozen dataclass), resolve().
4. All 7 tests in test_deferred.py pass.
5. No ruff or mypy errors introduced (run `uv run ruff check src/godoo_stateman/dsl/` and `uv run mypy src/godoo_stateman/dsl/types/`).
</success_criteria>

<output>
Create `.planning/phases/02-dsl-eval-pure-pipeline/02-01-SUMMARY.md` when done
</output>
