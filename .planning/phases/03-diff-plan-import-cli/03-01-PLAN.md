---
phase: 03-diff-plan-import-cli
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/godoo_stateman/dsl/types/desired.py
  - src/godoo_stateman/dsl/eval.py
  - src/godoo_stateman/dsl/context.py
  - src/godoo_stateman/dsl/normalize.py
  - src/godoo_stateman/errors.py
  - src/godoo_stateman/cli/commands/plan.py
  - tests/unit/dsl/test_eval.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SAFE-03
  - CORE-03
  - IDENT-01

must_haves:
  truths:
    - "DSL configs using `xmlid_prefix = '...'` evaluate successfully"
    - "DSL configs using the old `module = '...'` keyword raise MissingModuleError with updated message"
    - "All Phase-2 unit tests pass with the renamed attribute"
    - "ROADMAP SC-3 and REQUIREMENTS SAFE-03 wording describe Reject as xmlid-namespace collision (D-02)"
    - "D-05: rename module=/_module= to xmlid_prefix=/_xmlid_prefix= lands as Wave 0 before all other Phase-3 work"
    - "D-03: adoption happens only via explicit import; the ROADMAP SC-3 / REQUIREMENTS SAFE-03 wording correction is done in Task 2"
  artifacts:
    - path: "src/godoo_stateman/dsl/types/desired.py"
      provides: "DesiredState.xmlid_prefix field (renamed from .module)"
    - path: "src/godoo_stateman/errors.py"
      provides: "XmlidCollisionError and LiveStateFetchError subclasses"
    - path: ".planning/ROADMAP.md"
      provides: "SC-3 updated to xmlid-collision definition"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SAFE-03 updated to xmlid-collision definition"
  key_links:
    - from: "src/godoo_stateman/dsl/eval.py"
      to: "src/godoo_stateman/dsl/types/desired.py"
      via: "DesiredState(xmlid_prefix=module_raw, ...)"
      pattern: "xmlid_prefix=module"
    - from: "src/godoo_stateman/dsl/normalize.py"
      to: "src/godoo_stateman/dsl/types/desired.py"
      via: "state.xmlid_prefix"
      pattern: "state\\.xmlid_prefix"
---

<objective>
Rename the DSL `module` keyword to `xmlid_prefix` (D-05) across all Phase-2 shipped
code and tests, add new Phase-3 error subclasses to errors.py, and update the ROADMAP
SC-3 / REQUIREMENTS SAFE-03 wording to match the redefined Reject semantics (D-01/D-02/D-03).

Purpose: Wave 0 gatekeeper — every subsequent plan in Phase 3 builds on the corrected
`DesiredState.xmlid_prefix` surface. The doc corrections prevent the verifier from
flagging SC-3 as a failure.

Output: Renamed attribute in 5 source files, 2 new error classes, updated test
assertions in test_eval.py, corrected wording in ROADMAP.md and REQUIREMENTS.md.
</objective>

<execution_context>
@C:\Users\marc\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\marc\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-diff-plan-import-cli/03-CONTEXT.md
@.planning/phases/03-diff-plan-import-cli/03-PATTERNS.md

<interfaces>
From src/godoo_stateman/dsl/types/desired.py (current — rename target):
  class DesiredState(BaseModel):
      model_config = ConfigDict(frozen=True, arbitrary_types_allowed=True)
      module: str                                    # ← rename to xmlid_prefix
      resources: tuple[ResourceNode, ...]
      data_sources: tuple[DataSourceNode, ...]
      config_parameters: tuple[dict[str, str], ...]

From src/godoo_stateman/dsl/eval.py (lines 222-248, rename targets):
  module_raw = exec_locals.get("module")             # ← keep reading "module" from DSL
  ...
  return DesiredState(module=module, ...)             # ← rename kwarg to xmlid_prefix=module_raw
  # Error message: 'module = "<name>"' → 'xmlid_prefix = "<name>"'

From src/godoo_stateman/dsl/normalize.py (line 113, rename target):
  module=state.module                                # ← rename to xmlid_prefix=state.xmlid_prefix

From src/godoo_stateman/dsl/context.py (search for _module= usage):
  # _module= field on ResourceNode → _xmlid_prefix= DSL keyword in context proxy

From src/godoo_stateman/errors.py (lines 1-39, append targets):
  class StatemanError(Exception): ...
  class DslEvalError(StatemanError): ...
  class MissingModuleError(DslEvalError): ...       # keep class name, update message ref
  class CycleError(StatemanError): ...

From tests/unit/dsl/test_eval.py (lines 160, 375, rename targets):
  assert state.module == "my_project"               # → state.xmlid_prefix
  assert state.module == "test_mod"                 # → state.xmlid_prefix
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename module → xmlid_prefix in DSL types and evaluator (D-05)</name>
  <files>
    src/godoo_stateman/dsl/types/desired.py
    src/godoo_stateman/dsl/eval.py
    src/godoo_stateman/dsl/context.py
    src/godoo_stateman/dsl/normalize.py
    src/godoo_stateman/cli/commands/plan.py
    tests/unit/dsl/test_eval.py
  </files>
  <read_first>
    - src/godoo_stateman/dsl/types/desired.py — full file (rename target: .module field)
    - src/godoo_stateman/dsl/eval.py — lines 215-248 (DesiredState construction + MissingModuleError message)
    - src/godoo_stateman/dsl/context.py — search for _module= to find DSL keyword reading
    - src/godoo_stateman/dsl/normalize.py — line 113 (state.module reference)
    - src/godoo_stateman/cli/commands/plan.py — line 27-28 (state.module reference in stub)
    - tests/unit/dsl/test_eval.py — lines 155-165 and 370-380 (assertions on state.module)
    - .planning/phases/03-diff-plan-import-cli/03-PATTERNS.md — "desired.py (model rename)" section for exact before/after
  </read_first>
  <behavior>
    - Test 1: DSL config with `xmlid_prefix = "mymod"` evaluates to DesiredState with .xmlid_prefix == "mymod"
    - Test 2: DSL config with the old `module = "mymod"` keyword raises MissingModuleError (the rename is a DSL keyword change; the evaluator reads `xmlid_prefix` from exec_locals, not `module`)
    - Test 3: test_eval.py assertions `state.module == ...` updated to `state.xmlid_prefix == ...` and still pass
  </behavior>
  <action>
    In desired.py: rename the `module: str` field to `xmlid_prefix: str`. Update docstring to
    say "renamed from module (D-05)". Keep all other fields identical.

    In eval.py: change `module_raw = exec_locals.get("module")` to
    `module_raw = exec_locals.get("xmlid_prefix")` (and the exec_globals fallback similarly).
    Update the MissingModuleError message from 'module = "<name>"' to 'xmlid_prefix = "<name>"'.
    Change `DesiredState(module=module, ...)` to `DesiredState(xmlid_prefix=module, ...)`.
    Update the docstring comment referencing `module = "..."`.

    In context.py: locate where `_module=` is read from DSL keyword dict or ResourceProxy
    kwargs and rename the DSL keyword lookup from `_module` to `_xmlid_prefix`. The
    `ResourceNode.xmlid_module` internal field name does NOT change (it is an internal Python
    field, not a DSL keyword). Only the DSL-facing string `"_module"` key is renamed to
    `"_xmlid_prefix"`.

    In normalize.py line ~113: rename `module=state.module` to `xmlid_prefix=state.xmlid_prefix`
    in the DesiredState constructor call inside normalize().

    In cli/commands/plan.py: update the stub's `state.module` reference to `state.xmlid_prefix`.

    In tests/unit/dsl/test_eval.py: update both `assert state.module == ...` assertions to
    `assert state.xmlid_prefix == ...`. Also update any DSL fixture strings that use
    `module = "..."` to use `xmlid_prefix = "..."`.

    After edits: run `uv run ruff check src/ tests/` and `uv run mypy src/` to surface any
    missed callsites. Fix before proceeding.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/dsl/ -q</automated>
  </verify>
  <acceptance_criteria>
    - desired.py contains `xmlid_prefix: str` (not `module: str`)
    - eval.py reads `xmlid_prefix` from exec_locals/exec_globals (not `module`)
    - eval.py MissingModuleError message references `xmlid_prefix = "..."`
    - normalize.py uses `xmlid_prefix=state.xmlid_prefix`
    - test_eval.py assertions reference `state.xmlid_prefix`
    - `uv run pytest tests/unit/dsl/ -q` exits 0 (all Phase-2 DSL tests pass)
    - `uv run mypy src/ --ignore-missing-imports` exits 0 (no AttributeError on .module)
  </acceptance_criteria>
  <done>All Phase-2 DSL unit tests pass; `state.module` no longer referenced in any file under src/ or tests/</done>
</task>

<task type="auto">
  <name>Task 2: Add XmlidCollisionError + LiveStateFetchError to errors.py; update docs (D-02, D-03)</name>
  <files>
    src/godoo_stateman/errors.py
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - src/godoo_stateman/errors.py — full file (append after existing CycleError)
    - .planning/ROADMAP.md — Phase 3 SC-3 paragraph (wording to update)
    - .planning/REQUIREMENTS.md — SAFE-03 entry (wording to update)
    - .planning/phases/03-diff-plan-import-cli/03-PATTERNS.md — "errors.py" section for exact class pattern
    - .planning/phases/03-diff-plan-import-cli/03-CONTEXT.md — D-02 and D-03 for authoritative new wording
  </read_first>
  <action>
    In errors.py: append two new subclasses after CycleError, following the identical
    `__init__(self, message: str) -> None: super().__init__(message)` pattern:

    `XmlidCollisionError(StatemanError)` — raised when `find_by_xmlid` resolves to a
    record of a different model than the config declares (per D-02). Docstring: "The xmlid
    namespace is already owned by a different Odoo model. Plan action: Reject."

    `LiveStateFetchError(StatemanError)` — raised when a live Odoo fetch fails or returns
    unexpected results (e.g. DataSourceNode selector resolves 0 or >1 records per Pitfall 5).

    In .planning/ROADMAP.md, Phase 3 section SC-3: replace the current text "A resource in
    the DSL whose natural identity matches an existing unmanaged Odoo record (no xmlid)
    surfaces as `Reject` (adoption conflict)..." with: "A resource in the DSL whose
    `xmlid_prefix.slug` xmlid already exists in `ir.model.data` pointing to a record of a
    **different model** surfaces as `Reject` (xmlid-namespace collision); this is decided
    from `ir.model.data` alone. An unmanaged look-alike record with no xmlid is invisible
    to stateman and results in `Create`. (Redefined per D-01/D-02.)"

    In .planning/REQUIREMENTS.md, SAFE-03 entry: replace the current text with: "SAFE-03:
    `plan` and `apply` never silently adopt an unmanaged Odoo record. If the `xmlid_prefix.slug`
    xmlid in `ir.model.data` already points to a record of a different model than the config
    declares, the plan action is `Reject` (xmlid-namespace collision), not `Create` or `Update`,
    until the operator resolves the collision manually. Unmanaged look-alike records with no
    managed xmlid are invisible to stateman and result in `Create`. (D-01/D-02)"
  </action>
  <verify>
    <automated>uv run python -c "from godoo_stateman.errors import XmlidCollisionError, LiveStateFetchError; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - errors.py imports cleanly: XmlidCollisionError and LiveStateFetchError importable
    - Both classes inherit from StatemanError
    - ROADMAP SC-3 no longer mentions "natural identity" — references xmlid-namespace collision
    - REQUIREMENTS SAFE-03 no longer mentions "natural identity" — references xmlid-namespace collision
    - `uv run pytest tests/unit/ -q` exits 0 (no regressions from errors.py additions)
  </acceptance_criteria>
  <done>
    Two new error classes in errors.py; ROADMAP SC-3 and REQUIREMENTS SAFE-03 updated to
    the xmlid-collision definition so the verifier will pass SC-3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DSL author → eval.py | Config file executes under exec() with restricted builtins; trusted author, not a security sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | eval.py exec() restricted builtins | accept | Trust boundary is author-level; getattr is in allowlist by design (documented in eval.py module docstring). No new attack surface from rename. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; all dependencies already declared and audited in prior phases. |
</threat_model>

<verification>
Run full Phase-2 unit suite after both tasks complete:
  uv run pytest tests/unit/ -q

Confirm no reference to state.module remains:
  grep -rn "state\.module" src/ tests/

Confirm xmlid_prefix appears in desired.py:
  grep "xmlid_prefix" src/godoo_stateman/dsl/types/desired.py

Confirm new errors are importable:
  uv run python -c "from godoo_stateman.errors import XmlidCollisionError, LiveStateFetchError; print('ok')"
</verification>

<success_criteria>
- `uv run pytest tests/unit/ -q` exits 0 — all Phase-2 tests pass with renamed surface
- `grep -rn "state\.module" src/ tests/` returns 0 matches
- `grep "xmlid_prefix" src/godoo_stateman/dsl/types/desired.py` returns 1+ matches
- XmlidCollisionError and LiveStateFetchError importable from godoo_stateman.errors
- ROADMAP SC-3 and REQUIREMENTS SAFE-03 contain "xmlid-namespace collision" (not "natural identity")
</success_criteria>

<output>
Create `.planning/phases/03-diff-plan-import-cli/03-01-SUMMARY.md` when done.
</output>
