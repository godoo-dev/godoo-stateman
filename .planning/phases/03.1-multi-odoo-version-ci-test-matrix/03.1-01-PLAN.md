---
phase: "03.1"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md
autonomous: false
requirements:
  - VAL-03

must_haves:
  truths:
    - "D-10: testcontainers Postgres defaults (postgres:15-alpine) are accepted; phase is halted (not silently continued) if odoo:19.0 fails to start against postgres:15-alpine, triggering upstream `godoo-testcontainers` fix"
    - "odoo:19.0 Docker image is confirmed available on Docker Hub"
    - "A testcontainer using odoo:19.0 + postgres:15-alpine starts and reaches ready state"
    - "Phase is halted (not silently continued) if either check fails"
  artifacts:
    - path: ".planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md"
      provides: "Wave 0 completion flags set to true; upstream halt documented if triggered"
      contains: "wave_0_complete: true"
  key_links:
    - from: "docker manifest inspect odoo:19.0"
      to: "Phase 03.1 continuation"
      via: "human decision gate"
      pattern: "odoo:19.0"
---

<objective>
Verify that `odoo:19.0` exists on Docker Hub and that a 19.0 testcontainer starts cleanly against `postgres:15-alpine` before any code changes are made.

Purpose: If Odoo 19.0 is unavailable or incompatible with Postgres 15, the whole matrix phase is moot. This gate prevents investing in code changes that cannot be validated. Per D-09/D-10, failure here triggers an upstream `godoo-testcontainers` PR + phase block.

Output: Go/no-go signal for the rest of Phase 03.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify odoo:19.0 Docker image availability</name>
  <files>
    .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md — Wave 0 requirements checklist to update
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md — D-09, D-10 halt protocol
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 10 (failure protocol) and §Open Questions
  </read_first>
  <action>
    Run `docker manifest inspect odoo:19.0` to check Docker Hub for the image. Also run `docker manifest inspect odoo:18.0` to confirm 18.0 is available. These commands do NOT pull the image — they only query the registry manifest.

    If `odoo:19.0` is found (exit 0): record the image digest summary in a comment and proceed.
    If `odoo:19.0` is NOT found (non-zero exit or "manifest unknown" error): STOP. Do NOT proceed to Task 2 or any later wave. Document the failure in VALIDATION.md under Wave 0 Requirements, mark the phase BLOCKED, and return a halt message to the user with the exact error output. The resolution path per D-09 is: file an issue on godoo-dev/godoo-py, wait for the image, then resume.

    On success: update VALIDATION.md `wave_0_complete` frontmatter to remain `false` (Task 2 must also pass before it flips to `true`). Mark the `docker manifest inspect odoo:19.0` Wave 0 checkbox as `[x]`.
  </action>
  <verify>
    <automated>docker manifest inspect odoo:19.0 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    `docker manifest inspect odoo:19.0` exits 0 and prints a manifest block beginning with `{"schemaVersion":` or `"mediaType":`. The `odoo:18.0` check also exits 0. VALIDATION.md Wave 0 first checkbox is marked `[x]`.
  </acceptance_criteria>
  <done>
    Both `odoo:17.0`, `odoo:18.0`, `odoo:19.0` manifest checks return exit 0. Phase is confirmed unblocked on image availability.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke-test odoo:19.0 testcontainer startup</name>
  <read_first>
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md — Wave 0 smoke requirement
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 6 (container.py: ODOO_VERSION → odoo:{version}) and §Research Finding 10 (D-10 protocol)
    - tests/conftest.py — existing `odoo` fixture that will be called with ODOO_VERSION=19.0
    - tests/acceptance/test_snapshot.py — pick `test_schema_registry_get` (one of the simpler integration tests that only needs the container up)
  </read_first>
  <what-built>
    Run a single integration test with ODOO_VERSION=19.0 to verify that: (a) the testcontainer harness pulls and starts `odoo:19.0`, (b) `postgres:15-alpine` pairs with Odoo 19.0 without database-init failures, (c) the Odoo REST-ready wait loop completes within the 300-second timeout. This is the D-10 smoke: it does NOT require the fixture or plan.py fix to be in place (the test still hardcodes OdooVersion(17,0) at this point — that is fine; we only care about container startup).
  </what-built>
  <how-to-verify>
    Run: `ODOO_VERSION=19.0 uv run pytest tests/acceptance/test_snapshot.py::test_schema_registry_get -v -s --log-cli-level=ERROR`

    Expected output if healthy:
    - Container pulls `odoo:19.0` (or finds it cached from Task 1 pull).
    - pytest output shows `PASSED` for the test (the version assertion will say "17.0" still — ignore that for this smoke; the container started).
    - No `TimeoutError` from `_wait_for_odoo_ready`.
    - No Postgres "role" / "version" initialization error in the container logs.

    If the test fails with a container `TimeoutError` or a Postgres init error:
    1. Capture the full container logs shown in pytest output.
    2. Identify whether the root cause is Postgres 15/Odoo 19 incompatibility (look for PG version messages).
    3. HALT the phase. Post the error. Resolution: file issue on godoo-dev/godoo-py for a `godoo-testcontainers` Postgres-version-aware default. Do NOT proceed until a new `godoo-testcontainers` version is available.

    If the test passes (container starts, even if assertion says "17.0"): confirm go-ahead and update VALIDATION.md `wave_0_complete: true`. Mark all Wave 0 checkboxes `[x]`.
  </how-to-verify>
  <resume-signal>
    Type "go" if the container started and the test passed (assertion mismatch "17.0" is expected and not a blocker here). Type "blocked: [reason]" if the container failed to start.
  </resume-signal>
  <acceptance_criteria>
    `ODOO_VERSION=19.0 uv run pytest tests/acceptance/test_snapshot.py::test_schema_registry_get -v` exits 0 or exits with a test assertion failure (not a container startup error). VALIDATION.md `wave_0_complete: true` and all Wave 0 checkboxes marked `[x]`.
  </acceptance_criteria>
  <done>
    Wave 0 pre-flight complete: odoo:19.0 is available on Docker Hub AND starts against postgres:15-alpine without container-level failure. Phase 03.1 unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker Hub registry | Manifest queries go to external registry; no credentials involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Spoofing | `docker manifest inspect` fetching tampered manifest | accept | Low-value target; official Docker Hub odoo/ namespace; no auth secret at risk |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Unit suite (no container) green after this plan:
`uv run pytest -m "not integration" -q` — exits 0

VALIDATION.md frontmatter `wave_0_complete: true` set after Task 2 checkpoint is approved.
</verification>

<success_criteria>
1. `docker manifest inspect odoo:19.0` exits 0 — image confirmed on Docker Hub.
2. `ODOO_VERSION=19.0 uv run pytest tests/acceptance/test_snapshot.py::test_schema_registry_get -v` confirms container startup (exit 0 or assertion-only failure).
3. VALIDATION.md `wave_0_complete: true`.
4. No downstream wave is started before this plan is confirmed complete.
</success_criteria>

<output>
Create `.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-01-SUMMARY.md` when done.
</output>
