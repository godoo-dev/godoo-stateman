---
phase: "03.1"
plan: 2
type: execute
wave: 2
depends_on:
  - "03.1-01"
files_modified:
  - tests/conftest.py
  - src/godoo_stateman/cli/commands/plan.py
autonomous: true
requirements:
  - VAL-03

must_haves:
  truths:
    - "A session-scoped `odoo_version: OdooVersion` fixture exists in tests/conftest.py that parses ODOO_VERSION env (default '17.0') with regex r'^\\d+\\.\\d+$'"
    - "`plan.py` reads ODOO_VERSION from env using the same regex+parse pattern as snapshot.py:61-66 instead of constructing OdooVersion(17, 0) inline"
    - "Both files pass `uv run ruff check .` and `uv run mypy src/godoo_stateman` with zero errors"
  artifacts:
    - path: "tests/conftest.py"
      provides: "odoo_version session fixture"
      contains: "def odoo_version"
    - path: "src/godoo_stateman/cli/commands/plan.py"
      provides: "ODOO_VERSION env-driven OdooVersion construction"
      contains: "ODOO_VERSION"
  key_links:
    - from: "tests/conftest.py:odoo_version"
      to: "tests/acceptance/test_plan_import.py:_run_pipeline"
      via: "pytest fixture injection"
      pattern: "odoo_version.*OdooVersion"
    - from: "src/godoo_stateman/cli/commands/plan.py"
      to: "SchemaRegistry(client, _odoo_version)"
      via: "env-parsed OdooVersion"
      pattern: "ODOO_VERSION"
---

<objective>
Add the `odoo_version` pytest fixture (D-01) to `tests/conftest.py` and fix the hardcoded `OdooVersion(17, 0)` in `plan.py:90` (D-04). These two changes are the foundation that enables the test rewrites in Wave 2.

Purpose: Until the fixture exists, no acceptance test can be version-aware. Until `plan.py` reads `ODOO_VERSION` from env, the CLI itself silently runs against the wrong schema registry version in the 18.0/19.0 matrix entries.

Output: A session-scoped `odoo_version` fixture visible to all `tests/acceptance/` tests, and `plan.py` that derives `OdooVersion` from the `ODOO_VERSION` env var.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Source-verified from codebase. -->

From src/godoo_stateman/cli/commands/snapshot.py:61-66 (canonical ODOO_VERSION parsing pattern):
```python
odoo_version_str = os.environ.get("ODOO_VERSION", "17.0")
if not re.match(r"^\d+\.\d+$", odoo_version_str):
    raise typer.BadParameter("ODOO_VERSION must match N.N format (e.g. '17.0')")

parts = odoo_version_str.split(".")
odoo_version = OdooVersion(major=int(parts[0]), minor=int(parts[1]))
```

From tests/conftest.py:13-24 (existing session-scoped `odoo` fixture — new fixture must match this scope):
```python
@pytest.fixture(scope="session")
async def odoo() -> TestHarness:
    """Session-scoped TestHarness fixture that starts a real Odoo 17 + Postgres container.

    Used exclusively by ``@pytest.mark.integration`` acceptance tests.
    ...
    """
    async with TestHarness(snapshot=True) as h:
        yield h
```

From src/godoo_stateman/cli/commands/plan.py:90 (line to replace):
```python
registry = SchemaRegistry(client, OdooVersion(17, 0))
```
plan.py imports block (current — does NOT include `re`): asyncio, os, pathlib.Path, typer, and godoo/stateman modules.

From src/godoo_stateman/schema.version (OdooVersion type):
```python
class OdooVersion:
    def __init__(self, major: int, minor: int) -> None: ...
    def __str__(self) -> str: ...  # returns "17.0" format
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add odoo_version session fixture to tests/conftest.py (D-01)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py — read the full file; understand existing imports, existing `odoo` fixture, pytest-asyncio setup
    - src/godoo_stateman/cli/commands/snapshot.py — read lines 55–70 to confirm the exact parsing block verbatim (regex, default, error wording, OdooVersion constructor keyword args)
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md — "tests/conftest.py — add odoo_version session fixture (D-01)" section for adaptation notes
  </read_first>
  <behavior>
    - Test 1: With ODOO_VERSION="18.0" in env, `odoo_version` fixture returns OdooVersion(major=18, minor=0) and str(odoo_version) == "18.0"
    - Test 2: With ODOO_VERSION unset, fixture returns OdooVersion(major=17, minor=0) (default "17.0")
    - Test 3: With ODOO_VERSION="bad" in env, pytest.fail() is called (not a ValueError or typer error)
    - Test 4: scope="session" — fixture is session-scoped, matching the `odoo` fixture scope
  </behavior>
  <action>
    Read `tests/conftest.py` in full. Add the following to the existing import block at the top:
    - `import os` (if not already present)
    - `import re` (add — not currently present)
    - `from godoo_stateman.schema.version import OdooVersion` (add)

    After the existing `odoo` fixture definition, add the `odoo_version` session fixture. The fixture MUST:
    - Be `scope="session"` (per D-01 and Pitfall 3 — scope mismatch with `odoo` fixture)
    - Be synchronous (NOT async — only reads os.environ; no await needed)
    - Use `os.environ.get("ODOO_VERSION", "17.0")` as the env read (same default as snapshot.py)
    - Validate with `re.match(r"^\d+\.\d+$", odoo_version_str)` — exact same regex as snapshot.py:61
    - Call `pytest.fail(f"ODOO_VERSION must match N.N format (e.g. '17.0'), got: {odoo_version_str!r}")` on invalid input (NOT `typer.BadParameter` — no Typer context in tests)
    - Parse via `parts = odoo_version_str.split(".")` then `return OdooVersion(major=int(parts[0]), minor=int(parts[1]))` (keyword args)

    Return type annotation must be `OdooVersion` (not `Any`). Docstring must reference its source pattern (snapshot.py:61) and scope rationale.

    Do NOT change the existing `odoo` fixture or any other part of conftest.py.
  </action>
  <verify>
    <automated>uv run pytest -m "not integration" -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. `uv run pytest -m "not integration" -q` exits 0.
    2. `grep -n "def odoo_version" tests/conftest.py` returns a line in the file.
    3. `grep -n 'scope="session"' tests/conftest.py` shows at least 2 matches (both fixtures are session-scoped).
    4. `grep -n 'import re' tests/conftest.py` shows exactly 1 match.
    5. `grep -n 'pytest.fail' tests/conftest.py` shows the error path uses pytest.fail, not typer.BadParameter.
    6. `uv run ruff check tests/conftest.py` exits 0.
    7. `uv run mypy tests/conftest.py --ignore-missing-imports` exits 0 (or mypy is not run on test files per project config — check pyproject.toml).
  </acceptance_criteria>
  <done>
    `tests/conftest.py` exposes a `def odoo_version() -> OdooVersion:` fixture with `scope="session"`, correct regex validation, `pytest.fail()` error path, and `OdooVersion(major=..., minor=...)` return. Unit suite passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix plan.py hardcoded OdooVersion(17, 0) to read from ODOO_VERSION env (D-04)</name>
  <files>src/godoo_stateman/cli/commands/plan.py</files>
  <read_first>
    - src/godoo_stateman/cli/commands/plan.py — read the full file; identify import block (lines 1–35 approx), `_plan_impl` function, line 90 (`SchemaRegistry(client, OdooVersion(17, 0))`), and any existing error-handling pattern (console.print / return 1 vs typer.BadParameter)
    - src/godoo_stateman/cli/commands/snapshot.py — re-read lines 55–70 to copy the parsing block verbatim
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md — "plan.py — replace OdooVersion(17, 0) at line 90 (D-04)" section, specifically the error-style note about typer.BadParameter
  </read_first>
  <behavior>
    - Test 1: With ODOO_VERSION="18.0", SchemaRegistry is constructed with OdooVersion(major=18, minor=0)
    - Test 2: With ODOO_VERSION unset, OdooVersion(major=17, minor=0) is used (default "17.0")
    - Test 3: With ODOO_VERSION="bad", error is raised/returned before SchemaRegistry construction
  </behavior>
  <action>
    Read `src/godoo_stateman/cli/commands/plan.py` in full.

    Step 1 — Add `import re` to the imports block (after the existing `import os` line). Do NOT add any other new imports. `OdooVersion` is already imported; `os` is already imported.

    Step 2 — Locate line 90 (approximately): `registry = SchemaRegistry(client, OdooVersion(17, 0))`. This is inside `_plan_impl`.

    Step 3 — Insert the following 5-line parsing block IMMEDIATELY BEFORE the `registry = ...` line, replacing the `OdooVersion(17, 0)` inline construct:

    - Read `os.environ.get("ODOO_VERSION", "17.0")` into a local variable.
    - Validate with `re.match(r"^\d+\.\d+$", ...)` — same regex as snapshot.py:61.
    - On invalid input: use the error-output pattern already present in `_plan_impl` (either `typer.BadParameter` or `console.print("[red]...[/red]")` + return — match whatever the function uses for pre-client-connect errors; see PATTERNS.md note: `typer.BadParameter` is preferred for consistency with snapshot.py since typer is already imported).
    - Parse via `.split(".")` then `OdooVersion(major=int(...), minor=int(...))` into a local variable named `_odoo_version` (underscore prefix avoids shadowing the outer `odoo_version` name if any exists in scope).
    - Replace `SchemaRegistry(client, OdooVersion(17, 0))` with `SchemaRegistry(client, _odoo_version)`.

    Per D-04 (Claude's Discretion): do NOT extract a helper module. The 5-line block is an inline duplicate of snapshot.py — this is intentional for this phase.

    Do NOT change any other part of plan.py.
  </action>
  <verify>
    <automated>uv run ruff check src/godoo_stateman/cli/commands/plan.py && uv run mypy src/godoo_stateman --ignore-missing-imports 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -n "OdooVersion(17, 0)" src/godoo_stateman/cli/commands/plan.py` returns zero matches.
    2. `grep -n "ODOO_VERSION" src/godoo_stateman/cli/commands/plan.py` returns at least 1 match.
    3. `grep -n "import re" src/godoo_stateman/cli/commands/plan.py` returns exactly 1 match.
    4. `uv run ruff check src/godoo_stateman/cli/commands/plan.py` exits 0.
    5. `uv run mypy src/godoo_stateman --ignore-missing-imports` exits 0.
    6. `uv run pytest -m "not integration" -q` exits 0.
  </acceptance_criteria>
  <done>
    `plan.py` contains no `OdooVersion(17, 0)` literal. The `_plan_impl` function reads `ODOO_VERSION` env, validates it, and passes the parsed `_odoo_version` to `SchemaRegistry`. ruff and mypy pass clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ODOO_VERSION env → plan.py | Operator-supplied env var crosses into integer parsing and SchemaRegistry construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-02 | Tampering | `ODOO_VERSION` env var in `plan.py` — malformed string could cause crash or unexpected behavior | mitigate | `re.match(r"^\d+\.\d+$", ...)` anchored regex rejects non-numeric values before any split/int conversion (ASVS L1 V5 input validation). Error path raises `typer.BadParameter` before `SchemaRegistry` is constructed. |
| T-03.1-03 | Tampering | `ODOO_VERSION` env var in `odoo_version` pytest fixture — malformed value aborts test session | accept | `pytest.fail()` on bad input is the correct test-infrastructure behavior; no production surface; low severity. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Unit suite passes after both tasks:
`uv run pytest -m "not integration" -q` — exits 0

Lint and typecheck:
`uv run ruff check . && uv run mypy src/godoo_stateman --ignore-missing-imports` — both exit 0

Fixture exists:
`grep -n "def odoo_version" tests/conftest.py` — returns match

plan.py hardcode removed:
`grep -c "OdooVersion(17, 0)" src/godoo_stateman/cli/commands/plan.py` — returns 0
</verification>

<success_criteria>
1. `tests/conftest.py` has `odoo_version() -> OdooVersion` session-scoped fixture with regex `r"^\d+\.\d+$"`, `pytest.fail()` error path, `OdooVersion(major=..., minor=...)` return.
2. `plan.py` has `import re` and reads `ODOO_VERSION` from env before constructing `SchemaRegistry`. Zero `OdooVersion(17, 0)` literals remain.
3. ruff + mypy both clean.
4. Unit suite green.
</success_criteria>

<output>
Create `.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-02-SUMMARY.md` when done.
</output>
