---
phase: 03.1-multi-odoo-version-ci-test-matrix
plan: 2
subsystem: testing
tags: [pytest, fixtures, odoo-version, ci-matrix, env-parsing]

requires:
  - phase: 03.1-01
    provides: Wave 0 pre-flight — confirmed odoo:19.0 exists on Docker Hub and boots cleanly

provides:
  - odoo_version session-scoped pytest fixture in tests/conftest.py parsing ODOO_VERSION env
  - plan.py reads ODOO_VERSION env via validated regex before constructing SchemaRegistry

affects:
  - 03.1-03 (test_snapshot.py rewrite — injects odoo_version fixture to replace hardcoded OdooVersion calls)
  - 03.1-04 (test_plan_import.py rewrite — _run_pipeline() consumes odoo_version fixture)

tech-stack:
  added: []
  patterns:
    - "ODOO_VERSION env-var as single source of truth: same regex r'^\\d+\\.\\d+$' and default '17.0' used in snapshot.py, plan.py, and the test fixture"
    - "pytest.fail() for test-infrastructure env errors (not typer.BadParameter — no Typer context in tests)"
    - "Inline duplication of 5-line parsing block deferred to extraction — acceptable until 3+ live call sites"

key-files:
  created: []
  modified:
    - tests/conftest.py
    - src/godoo_stateman/cli/commands/plan.py

key-decisions:
  - "D-01 implemented: odoo_version fixture is session-scoped (not function-scoped) to match the odoo fixture scope and avoid container-per-test overhead"
  - "D-04 implemented: OdooVersion(17, 0) hardcode removed from plan.py; typer.BadParameter used for error path (consistent with snapshot.py since typer is already imported in plan.py)"
  - "Inline duplicate of snapshot.py:61 parsing block in plan.py — extraction deferred per D-04 discretion note"

patterns-established:
  - "Pattern: ODOO_VERSION env parsing — regex r'^\\d+\\.\\d+$', default '17.0', OdooVersion(major=int(...), minor=int(...)) with keyword args — canonical in snapshot.py:61, replicated in plan.py and conftest.py"

requirements-completed:
  - VAL-03

duration: 3min
completed: 2026-05-28
---

# Phase 03.1 Plan 2: Add odoo_version Fixture and Fix plan.py Hardcode Summary

**Session-scoped odoo_version pytest fixture added parsing ODOO_VERSION env with anchored regex; plan.py hardcoded OdooVersion(17, 0) replaced with the same env-driven pattern — enabling Wave 2 test rewrites.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-28T13:16:46Z
- **Completed:** 2026-05-28T13:19:30Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `odoo_version() -> OdooVersion` session-scoped fixture to `tests/conftest.py` that parses `ODOO_VERSION` env with the same regex as `snapshot.py:61` verbatim, calls `pytest.fail()` on invalid input, and returns `OdooVersion(major=..., minor=...)` with keyword args
- Removed the only remaining hardcoded `OdooVersion(17, 0)` from `plan.py` (line 90); replaced with 5-line inline parsing block matching `snapshot.py:61`, with `typer.BadParameter` error path and `import re` added
- Unit suite (165 tests) stays green; ruff and mypy strict both clean on all 33 source files

## Task Commits

Each task was committed atomically:

1. **Task 1: Add odoo_version session fixture to tests/conftest.py (D-01)** - `faeaa6d` (feat)
2. **Task 2: Fix plan.py hardcoded OdooVersion(17, 0) to read from ODOO_VERSION env (D-04)** - `85bebab` (fix)

**Plan metadata:** see final commit below

## Files Created/Modified

- `tests/conftest.py` - Added `import os`, `import re`, `from godoo_stateman.schema.version import OdooVersion`; added `odoo_version()` session-scoped fixture after the existing `odoo` fixture
- `src/godoo_stateman/cli/commands/plan.py` - Added `import re`; inserted 5-line ODOO_VERSION parsing block before `SchemaRegistry` construction at line ~90; removed `OdooVersion(17, 0)` literal

## Decisions Made

- Used `pytest.fail()` (not `typer.BadParameter`) in the fixture — no Typer context exists in test execution; `pytest.fail()` aborts the session with a clear message
- Used `typer.BadParameter` in `plan.py` (not `console.print + return 1`) — consistent with `snapshot.py`'s error style, and `typer` is already imported in `plan.py`
- Named the local variable `_odoo_version` (underscore prefix) to avoid any name shadowing risk inside `_plan_impl`
- Extraction of the 5-line parsing block to a shared helper deferred per D-04 discretion — two call sites does not justify the module yet

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `tests/conftest.py` now exposes `odoo_version` fixture visible to both `tests/acceptance/test_snapshot.py` and `tests/acceptance/test_plan_import.py`
- Plan 03.1-03 can rewrite `test_snapshot.py` hardcodes (`OdooVersion(17, 0)` at lines ~45, 67, 85 and `== "17.0"` assertions) to use the fixture
- Plan 03.1-04 can rewrite `_run_pipeline()` in `test_plan_import.py` to accept the fixture
- No blockers

---
*Phase: 03.1-multi-odoo-version-ci-test-matrix*
*Completed: 2026-05-28*
