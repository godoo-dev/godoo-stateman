---
phase: "03.1"
plan: 3
type: execute
wave: 3
depends_on:
  - "03.1-02"
files_modified:
  - tests/acceptance/test_plan_import.py
  - tests/acceptance/test_snapshot.py
autonomous: true
requirements:
  - VAL-03

must_haves:
  truths:
    - "D-02: `_run_pipeline()` in `tests/acceptance/test_plan_import.py` accepts `odoo_version: OdooVersion` as the third positional parameter and passes it to `SchemaRegistry`; all 7 callers inject the fixture and pass it through (test_plan_is_deterministic passes it to both `_run_pipeline` calls)"
    - "D-03: the 3 hardcoded `OdooVersion(17, 0)` sites in `tests/acceptance/test_snapshot.py` (lines ~45, 67, 85) are replaced with the `odoo_version` fixture; `.odoo_version == '17.0'` assertions (lines ~49, 92, 94) read `== str(odoo_version)`; CARVE-OUT: `test_version_mismatch_on_load` retains its `'17.0'` JSON payload literal unchanged (it tests schema-format-version mismatch, not Odoo-version correctness)"
    - "Zero `OdooVersion(17, 0)` literals remain across the acceptance test suite"
  artifacts:
    - path: "tests/acceptance/test_plan_import.py"
      provides: "_run_pipeline signature updated; 7 callers updated"
      contains: "odoo_version: OdooVersion"
    - path: "tests/acceptance/test_snapshot.py"
      provides: "three SchemaRegistry calls updated; .odoo_version assertions updated"
      contains: "str(odoo_version)"
  key_links:
    - from: "tests/conftest.py:odoo_version"
      to: "tests/acceptance/test_plan_import.py:_run_pipeline"
      via: "pytest fixture injection into 7 test functions"
      pattern: "_run_pipeline.*odoo_version"
    - from: "tests/conftest.py:odoo_version"
      to: "tests/acceptance/test_snapshot.py:SchemaRegistry"
      via: "pytest fixture injection into 3 test functions"
      pattern: "SchemaRegistry.*odoo_version"
---

<objective>
Rewrite both acceptance test files to inject the `odoo_version` fixture instead of constructing `OdooVersion(17, 0)` inline (D-02, D-03). After this plan, the acceptance suite is fully version-aware: running `ODOO_VERSION=18.0 uv run pytest tests/acceptance -m integration` will exercise Odoo 18.0 schema paths.

Purpose: Without this plan the CI matrix runs all three version entries against the same hardcoded OdooVersion(17, 0) — the 18.0 and 19.0 entries are structurally meaningless.

Output: Both test files with zero `OdooVersion(17, 0)` literals; all callers of `_run_pipeline()` pass `odoo_version`; `test_version_mismatch_on_load` JSON payload `"17.0"` literal is preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-02-SUMMARY.md

<interfaces>
<!-- Source-verified contracts from PATTERNS.md and RESEARCH.md -->

Current _run_pipeline signature (test_plan_import.py lines ~122-125):
```python
async def _run_pipeline(
    client: Any,
    state: DesiredState,
) -> tuple[list[PlanStep], nx.DiGraph]:
    ...
    registry = SchemaRegistry(client, OdooVersion(17, 0))  # line ~135 — to be replaced
```

Target _run_pipeline signature (D-02):
```python
async def _run_pipeline(
    client: Any,
    state: DesiredState,
    odoo_version: OdooVersion,      # new third positional parameter
) -> tuple[list[PlanStep], nx.DiGraph]:
    ...
    registry = SchemaRegistry(client, odoo_version)
```

All 7 callers in test_plan_import.py that must have odoo_version added:
- test_plan_shows_create_for_new_resource (~line 202)
- test_plan_shows_noop_after_create (~line 236)
- test_plan_shows_update_on_field_change (~line 269)
- test_plan_is_deterministic (~line 305 — calls _run_pipeline TWICE)
- test_plan_reject_on_xmlid_collision (~line 345)
- test_import_then_plan_shows_managed (~line 523)
- test_plan_lists_managed_set (~line 567)

Fixture injection caller pattern (from PATTERNS.md):
```python
@pytest.mark.integration
async def test_plan_shows_create_for_new_resource(
    cleanup_xmlids: Any,
    odoo_version: OdooVersion,      # inject fixture here
) -> None:
    ...
    plan_steps, _graph = await _run_pipeline(client, state, odoo_version)
```

Three OdooVersion(17, 0) hardcodes in test_snapshot.py (lines ~45, 67, 85):
```python
registry = SchemaRegistry(odoo.client, OdooVersion(17, 0))  # type: ignore[attr-defined]
# Replace with:
registry = SchemaRegistry(odoo.client, odoo_version)  # type: ignore[attr-defined]
```

Four assertion replacements in test_snapshot.py:
```python
# Line ~49: assert schema.odoo_version == "17.0"
# Replace:  assert schema.odoo_version == str(odoo_version)

# Line ~85 VersionedSnapshot.load call:
# loaded = VersionedSnapshot.load(path, "17.0")
# Replace:  loaded = VersionedSnapshot.load(path, str(odoo_version))

# Lines ~92, 94: assert loaded.odoo_version == "17.0"
# Replace:        assert loaded.odoo_version == str(odoo_version)
```

CRITICAL CARVE-OUT — do NOT change test_version_mismatch_on_load:
```python
# Lines ~116-121 — test payload, NOT an Odoo-version assertion:
snapshot_data = {
    "odoo_version": "17.0",      # MUST stay "17.0" — this is the test fixture value
    "schema_format_version": 999,
    ...
}
VersionedSnapshot.load(path, "17.0")   # MUST stay "17.0" — also a fixture value
```

Tests in test_snapshot.py that need odoo_version injected (add to function signature):
- test_schema_registry_get(odoo: object) → add odoo_version: OdooVersion
- test_store_flag_regression(odoo: object) → add odoo_version: OdooVersion
- test_snapshot_round_trip(odoo: object, tmp_path: Path) → add odoo_version: OdooVersion

Tests that do NOT need odoo_version injected:
- test_version_mismatch_on_load — no OdooVersion constructor, no .odoo_version assertion
- test_xmlid_round_trip — no OdooVersion constructor
- test_xmlid_idempotent — no OdooVersion constructor
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite _run_pipeline and its 7 callers in test_plan_import.py (D-02)</name>
  <files>tests/acceptance/test_plan_import.py</files>
  <read_first>
    - tests/acceptance/test_plan_import.py — read the full file; locate _run_pipeline definition (~line 122), its 7 callers listed in the interfaces block above, and their existing async def signatures
    - tests/conftest.py — confirm odoo_version fixture was added in Plan 02 (verify `def odoo_version` exists)
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md — "test_plan_import.py — parameterize _run_pipeline() on odoo_version (D-02)" section; pay attention to the Pitfall 2 warning about missing callers
  </read_first>
  <behavior>
    - Test 1: _run_pipeline signature has 3 parameters: client, state, odoo_version
    - Test 2: All 7 callers inject odoo_version from fixture and pass it as the third arg
    - Test 3: test_plan_is_deterministic passes odoo_version to BOTH _run_pipeline calls
    - Test 4: No OdooVersion(17, 0) literal remains in test_plan_import.py
  </behavior>
  <action>
    Read `tests/acceptance/test_plan_import.py` in full.

    Step 1 — Update `_run_pipeline` helper (lines ~122-140):
    - Add `odoo_version: OdooVersion` as the third positional parameter after `state: DesiredState`.
    - Replace `SchemaRegistry(client, OdooVersion(17, 0))` with `SchemaRegistry(client, odoo_version)` on the line inside the helper.
    - The `OdooVersion` import may still be needed by other parts of the file — check before removing it. If `OdooVersion` is only used in the hardcode site, remove the import to keep the file clean; if used elsewhere, leave it.

    Step 2 — Update all 7 callers. For each test function that calls `_run_pipeline`:
    - Add `odoo_version: OdooVersion` to the `async def test_*(...)` function parameter list (after existing params like `cleanup_xmlids: Any`).
    - Update the `_run_pipeline(client, state)` call to `_run_pipeline(client, state, odoo_version)`.
    - For `test_plan_is_deterministic` which calls `_run_pipeline` twice: both calls must pass `odoo_version`.

    The 7 callers are:
    1. test_plan_shows_create_for_new_resource
    2. test_plan_shows_noop_after_create
    3. test_plan_shows_update_on_field_change
    4. test_plan_is_deterministic (two calls)
    5. test_plan_reject_on_xmlid_collision
    6. test_import_then_plan_shows_managed
    7. test_plan_lists_managed_set

    Do NOT change any other test functions (test_datasource_*, test_import_writes_xmlid, test_import_idempotent, test_import_collision_requires_force — these do not call `_run_pipeline`).

    After edits: run `uv run ruff check tests/acceptance/test_plan_import.py` to catch any import issues.
  </action>
  <verify>
    <automated>grep -c "OdooVersion(17, 0)" tests/acceptance/test_plan_import.py</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -c "OdooVersion(17, 0)" tests/acceptance/test_plan_import.py` returns 0.
    2. `grep -c "odoo_version: OdooVersion" tests/acceptance/test_plan_import.py` returns at least 8 (1 in _run_pipeline signature + 7 in test function signatures).
    3. `grep -c "_run_pipeline(client, state, odoo_version)" tests/acceptance/test_plan_import.py` returns at least 8 (7 callers + 1 in test_plan_is_deterministic makes 8 total call sites — or at least 7 if the double-call test uses a variable).
    4. `uv run ruff check tests/acceptance/test_plan_import.py` exits 0.
    5. `uv run pytest -m "not integration" -q` exits 0 (unit suite still green).
  </acceptance_criteria>
  <done>
    `test_plan_import.py` contains zero `OdooVersion(17, 0)` literals. `_run_pipeline` accepts `odoo_version: OdooVersion`. All 7 callers inject the fixture and pass it through.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject odoo_version fixture at three sites in test_snapshot.py (D-03)</name>
  <files>tests/acceptance/test_snapshot.py</files>
  <read_first>
    - tests/acceptance/test_snapshot.py — read the full file; locate the three OdooVersion(17, 0) sites (lines ~45, 67, 85), the four "17.0" assertion sites (lines ~49, 92, 94), the VersionedSnapshot.load call at ~line 85, and the test_version_mismatch_on_load function body (~lines 110-130) to confirm the carve-out
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md — "test_snapshot.py — inject odoo_version at three hardcode sites (D-03)" section; especially the CRITICAL CARVE-OUT block
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 4 "Important clarification" paragraph about which "17.0" literals change vs which must stay
  </read_first>
  <behavior>
    - Test 1: SchemaRegistry in test_schema_registry_get receives odoo_version from fixture, not OdooVersion(17, 0)
    - Test 2: schema.odoo_version assertion uses str(odoo_version), not "17.0" literal
    - Test 3: test_version_mismatch_on_load still contains "17.0" as JSON payload value (unchanged)
    - Test 4: VersionedSnapshot.load in test_snapshot_round_trip uses str(odoo_version)
    - Test 5: Zero OdooVersion(17, 0) literals remain in test_snapshot.py
  </behavior>
  <action>
    Read `tests/acceptance/test_snapshot.py` in full.

    Step 1 — Update 3 test function signatures (add `odoo_version: OdooVersion` parameter):
    - `test_schema_registry_get(odoo: object)` → `test_schema_registry_get(odoo: object, odoo_version: OdooVersion)`
    - `test_store_flag_regression(odoo: object)` → `test_store_flag_regression(odoo: object, odoo_version: OdooVersion)`
    - `test_snapshot_round_trip(odoo: object, tmp_path: Path)` → `test_snapshot_round_trip(odoo: object, tmp_path: Path, odoo_version: OdooVersion)`

    Step 2 — Replace 3 `OdooVersion(17, 0)` constructor calls (one per test function above):
    - Each occurrence of `SchemaRegistry(odoo.client, OdooVersion(17, 0))` becomes `SchemaRegistry(odoo.client, odoo_version)`.
    - Preserve the `# type: ignore[attr-defined]` comment if it exists on those lines.

    Step 3 — Replace 4 `"17.0"` string literals that are Odoo-version assertions (NOT the JSON payload):
    - Line ~49: `assert schema.odoo_version == "17.0"` → `assert schema.odoo_version == str(odoo_version)`
    - The `VersionedSnapshot.load(path, "17.0")` call at line ~85 inside `test_snapshot_round_trip` → `VersionedSnapshot.load(path, str(odoo_version))`
    - Lines ~92, 94: `assert loaded.odoo_version == "17.0"` → `assert loaded.odoo_version == str(odoo_version)`

    CRITICAL: Do NOT change `test_version_mismatch_on_load`. That function constructs a dict with `"odoo_version": "17.0"` and calls `VersionedSnapshot.load(path, "17.0")` — both are test fixture values that deliberately test schema-format-version mismatch behavior. Changing them would break the test semantics.

    Also do NOT add `odoo_version` parameter to `test_version_mismatch_on_load`, `test_xmlid_round_trip`, or `test_xmlid_idempotent` — they don't need it.

    Check whether `OdooVersion` import is still needed in `test_snapshot.py` after removing the 3 constructor calls. If `OdooVersion` is no longer referenced, remove the import. If it's referenced in type annotations of existing parameters, leave it.

    After edits: `uv run ruff check tests/acceptance/test_snapshot.py`.
  </action>
  <verify>
    <automated>grep -c "OdooVersion(17, 0)" tests/acceptance/test_snapshot.py</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -c "OdooVersion(17, 0)" tests/acceptance/test_snapshot.py` returns 0.
    2. `grep -c "str(odoo_version)" tests/acceptance/test_snapshot.py` returns at least 4 (one per replaced assertion + the VersionedSnapshot.load call).
    3. `grep -n '"17.0"' tests/acceptance/test_snapshot.py` — remaining "17.0" literals are ONLY inside `test_version_mismatch_on_load` (2 occurrences for the JSON payload + VersionedSnapshot.load fixture call). Zero "17.0" literals remain outside that function.
    4. `uv run ruff check tests/acceptance/test_snapshot.py` exits 0.
    5. `uv run pytest -m "not integration" -q` exits 0.
  </acceptance_criteria>
  <done>
    `test_snapshot.py` has zero `OdooVersion(17, 0)` literals. The three `SchemaRegistry` calls use `odoo_version` from fixture. The four `.odoo_version` assertions use `str(odoo_version)`. The `test_version_mismatch_on_load` function is unchanged and still contains `"17.0"` payload literals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No production surface involved — test file edits only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-04 | n/a | test-only file changes | accept | Informational test-only changes; no executable production surface; no threats |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Acceptance criteria guards against Pitfall 1 (over-editing carve-out) and Pitfall 2 (missing callers):

Pitfall 1 guard — carve-out preserved:
`grep -n '"17.0"' tests/acceptance/test_snapshot.py` — all remaining matches are inside test_version_mismatch_on_load

Pitfall 2 guard — all 7 callers updated:
`grep -c "odoo_version: OdooVersion" tests/acceptance/test_plan_import.py` — returns ≥ 8

Full acceptance test pre-check (unit suite, no container):
`uv run pytest -m "not integration" -q` — exits 0

Lint:
`uv run ruff check tests/acceptance/` — exits 0
</verification>

<success_criteria>
1. `grep -c "OdooVersion(17, 0)" tests/acceptance/test_plan_import.py tests/acceptance/test_snapshot.py` returns 0 across both files.
2. `test_version_mismatch_on_load` still contains `"odoo_version": "17.0"` JSON payload (confirmed unchanged).
3. All 7 `_run_pipeline` callers in `test_plan_import.py` pass `odoo_version` as third argument.
4. `test_schema_registry_get`, `test_store_flag_regression`, `test_snapshot_round_trip` each have `odoo_version: OdooVersion` in their signatures.
5. ruff clean across both acceptance test files.
6. Unit suite green.
</success_criteria>

<output>
Create `.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-03-SUMMARY.md` when done.
</output>
