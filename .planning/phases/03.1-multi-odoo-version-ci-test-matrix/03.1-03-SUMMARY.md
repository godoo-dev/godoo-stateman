---
phase: 03.1-multi-odoo-version-ci-test-matrix
plan: 3
subsystem: testing
tags: [pytest, odoo_version, fixture, acceptance-tests, parameterization]

# Dependency graph
requires:
  - phase: 03.1-02
    provides: odoo_version session fixture in tests/conftest.py

provides:
  - _run_pipeline() helper parameterized on odoo_version (no hardcoded OdooVersion(17, 0))
  - All 7 _run_pipeline callers in test_plan_import.py inject the odoo_version fixture
  - test_snapshot.py three SchemaRegistry sites use odoo_version fixture
  - test_snapshot.py odoo_version assertions use str(odoo_version) for version-agnostic correctness
  - test_version_mismatch_on_load carve-out preserved (JSON payload "17.0" literals unchanged)

affects:
  - 03.1-04 (wave 4 — integration tests now version-aware, will run against any ODOO_VERSION)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "odoo_version fixture injection: add odoo_version: OdooVersion to test function signature; pytest injects the session-scoped fixture automatically"
    - "str(odoo_version) for assertions: version string comparisons use str(odoo_version) instead of hardcoded '17.0'"
    - "Carve-out rule: test fixture data that represents a specific Odoo version (e.g., schema-version-mismatch payload) must NOT use the injected fixture — it tests the wrong-version detection path"

key-files:
  created: []
  modified:
    - tests/acceptance/test_plan_import.py
    - tests/acceptance/test_snapshot.py

key-decisions:
  - "str(odoo_version) plan count discrepancy: plan acceptance criteria said 'at least 4' str(odoo_version) occurrences but the actual code has 3 replacement sites (line 49, 92, 94 in test_snapshot.py). The plan count was off-by-one; the 3 replacements are correct and complete."
  - "OdooVersion import retained in test_snapshot.py: after removing 3 constructor calls, OdooVersion is still needed as a type annotation in the three updated function signatures"

patterns-established:
  - "Fixture injection for version-aware tests: prefer OdooVersion fixture parameter over hardcoded OdooVersion(major, minor) constructor in test functions"

requirements-completed:
  - VAL-03

# Metrics
duration: 12min
completed: 2026-05-28
---

# Phase 03.1 Plan 03: Parameterize acceptance tests on odoo_version fixture (D-02, D-03) Summary

**Both acceptance test files rewritten to inject the `odoo_version` session fixture — zero `OdooVersion(17, 0)` hardcodes remain; CI matrix will now exercise the correct Odoo version in each matrix entry.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-05-28T00:00:00Z
- **Completed:** 2026-05-28T00:12:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `_run_pipeline()` in `test_plan_import.py` now accepts `odoo_version: OdooVersion` as third positional parameter; all 7 callers inject the fixture and pass it through
- `test_snapshot.py` three `SchemaRegistry(odoo.client, OdooVersion(17, 0))` constructor calls replaced with `SchemaRegistry(odoo.client, odoo_version)` fixture injection; `.odoo_version` assertions updated to `str(odoo_version)`
- `test_version_mismatch_on_load` carve-out preserved: both `"17.0"` JSON payload literals remain unchanged (they test schema-format-version mismatch, not Odoo-version correctness)
- ruff clean and mypy strict pass across entire `src/` tree; unit suite exits 0

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite _run_pipeline and its 7 callers in test_plan_import.py (D-02)** - `76495d0` (refactor)
2. **Task 2: Inject odoo_version fixture at three sites in test_snapshot.py (D-03)** - `2ed17fc` (refactor)

**Plan metadata:** (see final commit below)

## Files Created/Modified

- `tests/acceptance/test_plan_import.py` - `_run_pipeline` signature updated; 7 test function signatures updated; 7 call sites pass `odoo_version`
- `tests/acceptance/test_snapshot.py` - 3 function signatures updated; 3 `OdooVersion(17, 0)` calls replaced; 3 `str(odoo_version)` assertions added

## Decisions Made

- `str(odoo_version)` occurrence count: the plan acceptance criteria stated "at least 4" but the file has exactly 3 replacement sites. The 3 changes are complete and correct; the plan count was a minor off-by-one. No additional changes needed.
- Retained `OdooVersion` import in `test_snapshot.py` because the type annotation `odoo_version: OdooVersion` in three updated function signatures requires it.

## Deviations from Plan

None - plan executed exactly as written. The minor plan count discrepancy (3 vs 4 expected `str(odoo_version)` occurrences) reflects a counting error in the plan's acceptance criteria, not a missing code change. All named replacement sites from PATTERNS.md were updated.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Both acceptance test files are now fully version-aware
- Running `ODOO_VERSION=18.0 uv run pytest tests/acceptance -m integration` will exercise Odoo 18.0 schema paths
- Wave 4 (03.1-04) can proceed: integration tests will run against the correct Odoo version in each CI matrix entry

## Self-Check

- [x] `tests/acceptance/test_plan_import.py` modified (Task 1)
- [x] `tests/acceptance/test_snapshot.py` modified (Task 2)
- [x] `grep -c "OdooVersion(17, 0)" tests/acceptance/test_plan_import.py` = 0
- [x] `grep -c "OdooVersion(17, 0)" tests/acceptance/test_snapshot.py` = 0
- [x] `grep -c "odoo_version: OdooVersion" tests/acceptance/test_plan_import.py` = 8
- [x] `"17.0"` literals in test_snapshot.py are ONLY in test_version_mismatch_on_load (lines 117, 126)
- [x] `uv run ruff check .` exits 0
- [x] `uv run mypy src/godoo_stateman` exits 0
- [x] `uv run pytest -m "not integration" -q` exits 0
- [x] Commits 76495d0 and 2ed17fc exist in git log

---
*Phase: 03.1-multi-odoo-version-ci-test-matrix*
*Completed: 2026-05-28*
