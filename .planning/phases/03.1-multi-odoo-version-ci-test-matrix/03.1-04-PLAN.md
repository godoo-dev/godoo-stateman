---
phase: "03.1"
plan: 4
type: execute
wave: 3
depends_on:
  - "03.1-02"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
requirements:
  - VAL-03

must_haves:
  truths:
    - "D-05: keep workflow filename `.github/workflows/test.yml` (no rename to `ci.yml`); ROADMAP Phase 03.1 SC-1 amended to read `test.yml` and `odoo_version` (underscore, not hyphen)"
    - "D-06: ROADMAP Phase 03.1 SC-2 drops the literal '18' test count and reads 'all `@pytest.mark.integration` tests' instead"
    - "D-07: REQUIREMENTS.md VAL-03 is rewritten to describe multi-Odoo-version validation (acceptance suite passes on 17.0/18.0/19.0 in CI matrix); the prior LOC-benchmark wording is moved to a new VAL-04 assigned to Phase 5; traceability table has VAL-03->Phase 03.1 and VAL-04->Phase 5; ROADMAP Phase 5 Requirements line and SC-5 reference VAL-04 instead of VAL-03"
    - "D-11: PROJECT.md and CLAUDE.md say 'tested against Odoo 17.0, 18.0, and 19.0' instead of 'Odoo 17+' / 'Odoo 17 CE is the primary target'; PROJECT.md historical decision record at line ~180 ('Odoo 17 first, but version seam NOW') is preserved unchanged"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected SC-1/SC-2 wording for Phase 03.1"
      contains: "test.yml"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Rewritten VAL-03 + new VAL-04"
      contains: "VAL-04"
    - path: ".planning/PROJECT.md"
      provides: "Updated Odoo version claims"
      contains: "17.0, 18.0, and 19.0"
    - path: "CLAUDE.md"
      provides: "Updated Odoo version claims"
      contains: "17.0, 18.0, and 19.0"
  key_links: []
---

<objective>
Apply all documentation and requirements corrections (D-05, D-06, D-07, D-11): fix ROADMAP success-criteria wording, rewrite REQUIREMENTS.md VAL-03 / add VAL-04, and update Odoo version claims in PROJECT.md and CLAUDE.md.

Purpose: These doc changes can be applied in parallel with the test rewrites (Plan 03) since they touch no code files. They close the traceability gap between what was planned and what exists in the repo.

Output: Four planning/doc files with accurate wording. No code is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md

<interfaces>
<!-- Exact replacement strings verified in PATTERNS.md §ROADMAP.md and §REQUIREMENTS.md -->

ROADMAP SC-1 current (line ~125):
  `.github/workflows/ci.yml` defines a job whose `strategy.matrix.odoo-version` is exactly `["17.0", "18.0", "19.0"]` with `fail-fast: false`, injects `ODOO_VERSION` as an env var, and runs the integration suite (`uv run pytest -m integration -q`) in each matrix entry.

ROADMAP SC-1 replacement (D-05):
  `.github/workflows/test.yml` defines an `integration` job whose `strategy.matrix.odoo_version` is exactly `["17.0", "18.0", "19.0"]` with `fail-fast: false`, injects `ODOO_VERSION` as an env var, and runs the integration suite (`uv run --no-sources pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR`) in each matrix entry.

ROADMAP SC-2 current (line ~126):
  All three matrix entries pass green on a sample PR — the 18 `@pytest.mark.integration` tests succeed against vanilla Odoo 17.0, 18.0, and 19.0 CE images (`odoo:17.0`, `odoo:18.0`, `odoo:19.0`) via testcontainers, with no per-test `skipif`/`xfail` introduced.

ROADMAP SC-2 replacement (D-06):
  All three matrix entries pass green on a sample PR — all `@pytest.mark.integration` tests succeed against vanilla Odoo 17.0, 18.0, and 19.0 CE images (`odoo:17.0`, `odoo:18.0`, `odoo:19.0`) via testcontainers, with no per-test `skipif`/`xfail` introduced.

REQUIREMENTS.md VAL-03 current (line 115):
  - [ ] **VAL-03**: The test suite meets or exceeds the TS `odoo-state-manager` benchmark of 29 test files / 4,564 LOC across unit, integration, and acceptance tests combined.

REQUIREMENTS.md VAL-03 replacement + new VAL-04 (D-07):
  - [ ] **VAL-03**: The acceptance suite passes on Odoo 17.0, 18.0, and 19.0 in the CI matrix. All `@pytest.mark.integration` tests run and pass green on all three matrix entries against vanilla Odoo CE images via testcontainers, with no per-test `skipif`/`xfail` introduced.
  - [ ] **VAL-04**: The test suite meets or exceeds the TS `odoo-state-manager` benchmark of 29 test files / 4,564 LOC across unit, integration, and acceptance tests combined.

REQUIREMENTS.md traceability table current:
  | VAL-03 | Phase 5 | Pending |

REQUIREMENTS.md traceability table replacement:
  | VAL-03 | Phase 03.1 | Pending |
  | VAL-04 | Phase 5 | Pending |

ROADMAP Phase 5 Requirements line current:
  CORE-07, CORE-09, RSRC-08, EXEC-01, EXEC-03, META-02, META-03, VAL-02, VAL-03

ROADMAP Phase 5 Requirements line replacement:
  CORE-07, CORE-09, RSRC-08, EXEC-01, EXEC-03, META-02, META-03, VAL-02, VAL-04

ROADMAP Phase 5 SC-5 current:
  The test suite reaches or exceeds 29 test files and 4,564 LOC across unit, integration, and acceptance tests combined (VAL-03 benchmark).

ROADMAP Phase 5 SC-5 replacement:
  The test suite reaches or exceeds 29 test files and 4,564 LOC across unit, integration, and acceptance tests combined (VAL-04 benchmark).

CLAUDE.md line 13 current:
  targeting the Odoo 17+ ecosystem via `godoo-py` for transport and introspection.

CLAUDE.md line 13 replacement (D-11):
  targeting the Odoo 17.0, 18.0, and 19.0 ecosystem via `godoo-py` for transport and introspection.

CLAUDE.md line 22 current:
  - **Odoo version**: Odoo 17 CE is the primary target. The schema-snapshot/registry layer must carry an explicit Odoo-version dimension from day one.

CLAUDE.md line 22 replacement (D-11):
  - **Odoo version**: Tested against Odoo 17.0, 18.0, and 19.0 (CE). The schema-snapshot/registry layer carries an explicit Odoo-version dimension.

PROJECT.md line 9 current:
  targeting the Odoo 17+ ecosystem via `godoo-py` for transport and introspection.

PROJECT.md line 9 replacement (D-11):
  targeting the Odoo 17.0, 18.0, and 19.0 ecosystem via `godoo-py` for transport and introspection.

PROJECT.md lines 163-164 current:
  - **Odoo version**: Odoo 17 CE is the primary target. The schema-snapshot/registry layer must carry an explicit Odoo-version dimension from day one.

PROJECT.md lines 163-164 replacement (D-11):
  - **Odoo version**: Tested against Odoo 17.0, 18.0, and 19.0 (CE). The schema-snapshot/registry layer carries an explicit Odoo-version dimension.

PROJECT.md line 24 current:
  - **Test harness**: Acceptance tests run against real Odoo 17 via testcontainers. Unit/integration split matches or exceeds TS benchmark (29 files / 4,564 LOC).

PROJECT.md line 24 replacement (D-11):
  - **Test harness**: Acceptance tests run against real Odoo 17.0, 18.0, and 19.0 via testcontainers. Unit/integration split matches or exceeds TS benchmark (29 files / 4,564 LOC).

DO NOT CHANGE PROJECT.md line 180 (historical decision record "Odoo 17 first, but version seam NOW").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md SC-1/SC-2 and Phase 5 VAL-03→VAL-04 references (D-05, D-06)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — read Phase 03.1 section (lines ~118-131) to locate exact SC-1/SC-2 text; read Phase 5 section (lines ~134-164) to locate Requirements line and SC-5 text
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md — "ROADMAP.md — SC-1 and SC-2 wording amendments (D-05, D-06)" section for exact before/after strings
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 8 "Secondary ROADMAP.md update" paragraph for the Phase 5 VAL-03→VAL-04 rename
  </read_first>
  <action>
    Read `.planning/ROADMAP.md` in full.

    Make exactly four targeted changes:

    1. Phase 03.1 SC-1 (line ~125): Replace the entire SC-1 sentence. The old sentence starts `.github/workflows/ci.yml defines a job whose \`strategy.matrix.odoo-version\`` — replace it with the version that says `test.yml`, `odoo_version` (underscore), and the correct test command `uv run --no-sources pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR`.

    2. Phase 03.1 SC-2 (line ~126): Replace only the literal `"the 18 \`@pytest.mark.integration\` tests"` fragment with `"all \`@pytest.mark.integration\` tests"` — the rest of the sentence is unchanged.

    3. Phase 5 `**Requirements:**` line: Replace `VAL-03` with `VAL-04` in the requirements list. The line reads `CORE-07, CORE-09, RSRC-08, EXEC-01, EXEC-03, META-02, META-03, VAL-02, VAL-03` — change only `VAL-03` at the end to `VAL-04`.

    4. Phase 5 SC-5: Replace `(VAL-03 benchmark)` with `(VAL-04 benchmark)`.

    Do NOT change any other section. Do NOT change the Phase 03.1 Plans, Goal, Depends on, or Reference lines.
  </action>
  <verify>
    <automated>grep -n "ci\.yml\|odoo-version\|the 18" .planning/ROADMAP.md | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -c "ci\.yml" .planning/ROADMAP.md` returns 0 (no remaining reference to ci.yml).
    2. `grep -c "test\.yml" .planning/ROADMAP.md` returns at least 2 (Reference line + SC-1).
    3. `grep -c "the 18" .planning/ROADMAP.md` returns 0 (no literal "18" test count).
    4. `grep -c "VAL-04" .planning/ROADMAP.md` returns at least 2 (Phase 5 Requirements + SC-5).
    5. `grep -c "VAL-03" .planning/ROADMAP.md` — VAL-03 still appears in Phase 03.1 `Requirements:` line and in Phase 5 old Requirements line; after the edit it should NOT appear in Phase 5 requirements or SC-5.
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 03.1 SC-1 references `test.yml` and `odoo_version`. SC-2 says "all @pytest.mark.integration tests" (no literal count). Phase 5 requirements and SC-5 reference VAL-04, not VAL-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite REQUIREMENTS.md VAL-03 + add VAL-04 + update traceability table (D-07)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read lines 109–120 (VAL section header + VAL-01/02/03 entries) and lines 244–262 (traceability table VAL rows)
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md — "REQUIREMENTS.md — VAL-03 rewrite + new VAL-04 (D-07)" section; the format reference showing VAL-01/VAL-02 as the formatting model
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 8 (exact wording + traceability table changes)
  </read_first>
  <action>
    Read `.planning/REQUIREMENTS.md` in full.

    Step 1 — VAL section (line 115): Replace the current VAL-03 line with the new VAL-03 line AND append VAL-04 on the next line. The current single line:

      `- [ ] **VAL-03**: The test suite meets or exceeds the TS ...`

    Becomes two lines:

      `- [ ] **VAL-03**: The acceptance suite passes on Odoo 17.0, 18.0, and 19.0 in the CI matrix. All \`@pytest.mark.integration\` tests run and pass green on all three matrix entries against vanilla Odoo CE images via testcontainers, with no per-test \`skipif\`/\`xfail\` introduced.`
      `- [ ] **VAL-04**: The test suite meets or exceeds the TS \`odoo-state-manager\` benchmark of 29 test files / 4,564 LOC across unit, integration, and acceptance tests combined.`

    Also update the VAL section description on line 111 from "against real Odoo 17 CE via testcontainers" to "against real Odoo 17.0, 18.0, and 19.0 CE via testcontainers" to reflect the multi-version scope.

    Step 2 — Traceability table (line ~251): Replace the single row:
      `| VAL-03 | Phase 5 | Pending |`

    With two rows:
      `| VAL-03 | Phase 03.1 | Pending |`
      `| VAL-04 | Phase 5 | Pending |`

    Keep the row order consistent with the existing table (VAL rows are near the bottom; insert VAL-04 row immediately after the new VAL-03 row).

    Do NOT change VAL-01, VAL-02, or any other section.
  </action>
  <verify>
    <automated>grep -n "VAL-03\|VAL-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -c "VAL-04" .planning/REQUIREMENTS.md` returns 2 (one in VAL section, one in traceability table).
    2. `grep -n "VAL-03" .planning/REQUIREMENTS.md` — VAL-03 entry now contains "17.0, 18.0, and 19.0" and "CI matrix", not "LOC".
    3. `grep -n "VAL-04" .planning/REQUIREMENTS.md` — VAL-04 entry contains "29 test files / 4,564 LOC".
    4. `grep -n "Phase 03.1" .planning/REQUIREMENTS.md` — appears in the traceability table VAL-03 row.
    5. `grep -n "Phase 5" .planning/REQUIREMENTS.md` — VAL-04 traceability row points to Phase 5 (VAL-02 also points to Phase 5; that is correct and unchanged).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md has a rewritten VAL-03 (multi-version validation, Phase 03.1) and a new VAL-04 (LOC benchmark, Phase 5). Traceability table has two rows for VAL-03 and VAL-04.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Odoo version claims in PROJECT.md and CLAUDE.md (D-11)</name>
  <files>
    .planning/PROJECT.md
    CLAUDE.md
  </files>
  <read_first>
    - .planning/PROJECT.md — read lines 1–35 (What This Is section, line 9 + Constraints ~line 24) and lines 155–185 (Constraints block ~163-164 + Key Decisions table ~180)
    - CLAUDE.md — read lines 1–30 (Project description line 13 + Constraints line 22)
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-PATTERNS.md — "PROJECT.md and CLAUDE.md — wording substitution (D-11)" section; especially the "DO NOT CHANGE" note for PROJECT.md line 180
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 9 for exact strings with line numbers
  </read_first>
  <action>
    Read both `.planning/PROJECT.md` and `CLAUDE.md` in full.

    Apply exactly 5 string replacements across the two files:

    PROJECT.md:
    1. Line ~9: `targeting the Odoo 17+ ecosystem` → `targeting the Odoo 17.0, 18.0, and 19.0 ecosystem`
    2. Lines ~163-164: `Odoo 17 CE is the primary target. The schema-snapshot/registry layer must carry an explicit Odoo-version dimension from day one.` → `Tested against Odoo 17.0, 18.0, and 19.0 (CE). The schema-snapshot/registry layer carries an explicit Odoo-version dimension.`
    3. Line ~24: `Acceptance tests run against real Odoo 17 via testcontainers.` → `Acceptance tests run against real Odoo 17.0, 18.0, and 19.0 via testcontainers.`

    CLAUDE.md:
    4. Line ~13: `targeting the Odoo 17+ ecosystem` → `targeting the Odoo 17.0, 18.0, and 19.0 ecosystem`
    5. Line ~22: `Odoo 17 CE is the primary target. The schema-snapshot/registry layer must carry an explicit Odoo-version dimension from day one.` → `Tested against Odoo 17.0, 18.0, and 19.0 (CE). The schema-snapshot/registry layer carries an explicit Odoo-version dimension.`

    DO NOT CHANGE: PROJECT.md line ~180 historical decision record `"Odoo 17 first, but version seam NOW"` — this is a historical record of the original architectural decision and must remain accurate to history.

    DO NOT CHANGE any other prose in either file.
  </action>
  <verify>
    <automated>grep -n "Odoo 17+" .planning/PROJECT.md CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -c "Odoo 17+" .planning/PROJECT.md` returns 0.
    2. `grep -c "Odoo 17+" CLAUDE.md` returns 0.
    3. `grep -c "17.0, 18.0, and 19.0" .planning/PROJECT.md` returns at least 3 (three replacements).
    4. `grep -c "17.0, 18.0, and 19.0" CLAUDE.md` returns at least 2 (two replacements).
    5. `grep -n "Odoo 17 CE is the primary target" .planning/PROJECT.md CLAUDE.md` returns 0 matches.
    6. `grep -n "Odoo 17 first" .planning/PROJECT.md` — the historical decision record at line ~180 is unchanged and still present.
    7. `grep -c "Tested against Odoo 17.0, 18.0, and 19.0 (CE)" CLAUDE.md` returns 1.
  </acceptance_criteria>
  <done>
    Both PROJECT.md and CLAUDE.md say "17.0, 18.0, and 19.0" in all forward-facing version claims. The historical decision record in PROJECT.md is unchanged. No "Odoo 17+" or "Odoo 17 CE is the primary target" remains in either file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Informational doc/requirements changes only — no executable surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | n/a | doc/requirements file changes | accept | Informational doc changes only; no executable surface; no threats |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
All doc string replacements confirmed:

ROADMAP SC-1 fixed:
`grep "ci\.yml" .planning/ROADMAP.md` — returns nothing

REQUIREMENTS VAL-04 added:
`grep "VAL-04" .planning/REQUIREMENTS.md` — returns 2 matches

No stale "Odoo 17+" wording:
`grep "Odoo 17+" .planning/PROJECT.md CLAUDE.md` — returns nothing

Unit suite unaffected:
`uv run pytest -m "not integration" -q` — exits 0
</verification>

<success_criteria>
1. ROADMAP SC-1: "test.yml" + "odoo_version" (underscore) + correct test command.
2. ROADMAP SC-2: "all @pytest.mark.integration tests" (no literal count).
3. ROADMAP Phase 5: requirements and SC-5 reference VAL-04.
4. REQUIREMENTS.md: VAL-03 = multi-version validation; VAL-04 = LOC benchmark.
5. Traceability table: VAL-03 → Phase 03.1, VAL-04 → Phase 5.
6. PROJECT.md + CLAUDE.md: zero "Odoo 17+" and zero "Odoo 17 CE is the primary target"; three+ occurrences of "17.0, 18.0, and 19.0".
</success_criteria>

<output>
Create `.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-04-SUMMARY.md` when done.
</output>
