---
phase: "03.1"
plan: 5
type: execute
wave: 4
depends_on:
  - "03.1-03"
  - "03.1-04"
files_modified:
  - tests/acceptance/test_plan_import.py
  - tests/acceptance/test_snapshot.py
  - src/godoo_stateman/cli/commands/plan.py
autonomous: false
requirements:
  - VAL-03

must_haves:
  truths:
    - "All 19 integration tests pass against Odoo 17.0 locally (baseline)"
    - "All 19 integration tests pass against Odoo 18.0 locally (shake-out result)"
    - "Either all 19 tests pass against Odoo 19.0, OR the phase is halted per D-09 with upstream issue filed"
    - "Zero skipif/xfail markers introduced — any failure is fixed in-phase (D-08)"
  artifacts:
    - path: "tests/acceptance/test_plan_import.py"
      provides: "passes on 17.0, 18.0, and 19.0"
      contains: "odoo_version: OdooVersion"
    - path: "tests/acceptance/test_snapshot.py"
      provides: "passes on 17.0, 18.0, and 19.0"
      contains: "str(odoo_version)"
  key_links:
    - from: "ODOO_VERSION env"
      to: "tests/acceptance (all integration tests)"
      via: "odoo_version fixture + testcontainer"
      pattern: "ODOO_VERSION=1[789]"
---

<objective>
Run the full acceptance suite against Odoo 17.0, 18.0, and 19.0 locally and fix any genuine test failures. This is the in-phase shake-out required by D-08: all three versions must pass green without skipif/xfail markers.

Purpose: The code changes in Plans 02 and 03 are necessary but not sufficient — we must verify that the acceptance tests actually pass when the containers start with real 18.0/19.0 images. Failures are fixed here, in-phase.

Output: A fully green local integration suite across all three Odoo versions, or a documented halt with upstream issue filed (D-09 protocol).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-03-SUMMARY.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-04-SUMMARY.md
@.planning/phases/03-diff-plan-import-cli/03-VERIFICATION.md

<interfaces>
<!-- Shake-out run commands and triage guidance from RESEARCH.md §7 -->

Run commands (sequential — run one version, fix failures, then proceed):

Odoo 17.0 baseline (should already pass):
  ODOO_VERSION=17.0 uv run pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR

Odoo 18.0 shake-out:
  ODOO_VERSION=18.0 uv run pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR

Odoo 19.0 shake-out:
  ODOO_VERSION=19.0 uv run pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR

Expected test count per run: 19 tests (12 in test_plan_import.py + 7 in test_snapshot.py).
A run reporting < 19 collected is a false-positive — investigate conftest or marker issue.

Models used (most stable in Odoo base):
  res.partner: name, active, country_id, display_name (store=False)
  res.country: code (selector field)
  ir.model.data: module, name, res_id, complete_name

Triage matrix (from RESEARCH.md §7):
  - ALL tests: PASS probability HIGH based on field stability analysis
  - HIGHEST RISK: Container startup (odoo:19.0 + postgres:15-alpine) — already verified in Wave 0

D-08 policy: If a test fails on 18/19, identify root cause. Fix in-phase.
  ALLOWED fixes: update test assertions, update production code for schema delta
  FORBIDDEN: skipif/xfail markers, per-test version guards

D-09 halt protocol (upstream bug):
  If failure is in godoo-client / godoo-introspection / godoo-testcontainers — NOT a stateman issue:
    1. Do NOT work around locally
    2. File issue on godoo-dev/godoo-py with reproduction steps
    3. Pin known-good version in pyproject.toml if needed
    4. Mark phase BLOCKED in STATE.md
    5. Return halt message to user
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run 17.0 baseline and 18.0 shake-out; fix any failures (D-08)</name>
  <files>
    tests/acceptance/test_plan_import.py
    tests/acceptance/test_snapshot.py
  </files>
  <read_first>
    - .planning/phases/03-diff-plan-import-cli/03-VERIFICATION.md — baseline of 19 passed integration tests on Odoo 17 CE; use as the "before" reference
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 7 (shake-out risk map, triage matrix) — understand which fields/models are in use
    - tests/acceptance/test_plan_import.py — re-read after Plan 03 edits; understand all 12 test bodies
    - tests/acceptance/test_snapshot.py — re-read after Plan 03 edits; understand all 7 test bodies
  </read_first>
  <action>
    Step 1 — Run 17.0 baseline:
    `ODOO_VERSION=17.0 uv run pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR`
    Expected: 19 passed. If any test fails here, the Plan 03 edits introduced a regression — fix before proceeding to 18.0.

    Step 2 — Run 18.0 shake-out:
    `ODOO_VERSION=18.0 uv run pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR`
    Expected: 19 passed. If failures occur:
      a. Read the full error output carefully.
      b. Identify whether the failure is a stateman test/code issue or an upstream godoo-py issue.
      c. If stateman issue: apply a minimal targeted fix to the failing test or production code. No skipif/xfail per D-08.
      d. If upstream godoo-py bug: apply D-09 protocol (halt + file issue). Do NOT proceed to 19.0 shake-out in that case.
      e. Re-run 18.0 after each fix attempt. Converge to all-green.

    Types of 18/19 failures that are fixable in-phase:
    - Assertion mismatch because a base-model field value changed (e.g., display_name format differs) → update assertion
    - Schema field presence differs → update test selector or expected field list
    - OdooVersion version string format mismatch in assertion → verify str(odoo_version) usage is correct

    Types of failures that trigger D-09 halt:
    - `godoo-client` CRUD helper raises unexpected exception
    - `godoo-introspection` Introspector returns wrong schema for a basic field
    - `godoo-testcontainers` container fails to start (already guarded in Wave 0 but could appear for 18.0 too)

    After all 17.0 + 18.0 fixes converge: commit with `fix(03.1): align acceptance tests for Odoo 17/18`.
  </action>
  <verify>
    <automated>ODOO_VERSION=17.0 uv run pytest tests/acceptance -m integration -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    1. `ODOO_VERSION=17.0 uv run pytest tests/acceptance -m integration -q` exits 0 with "19 passed" (or current count if test count changed).
    2. `ODOO_VERSION=18.0 uv run pytest tests/acceptance -m integration -q` exits 0 with same count.
    3. `grep -rn "skipif\|xfail" tests/acceptance/` returns 0 matches (no new skip markers added).
    4. `uv run ruff check tests/acceptance/` exits 0.
  </acceptance_criteria>
  <done>
    Both Odoo 17.0 and 18.0 acceptance suites pass green with the same test count. Zero skipif/xfail markers in the test files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run 19.0 shake-out and confirm result or invoke D-09 halt</name>
  <read_first>
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md — D-09 (upstream bug halt protocol) + D-10 (Postgres defaults)
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-RESEARCH.md — §Research Finding 10 (Postgres 15/Odoo 19 failure protocol); §Open Question 2 (Postgres 15 + Odoo 19 compatibility)
  </read_first>
  <what-built>
    Run `ODOO_VERSION=19.0 uv run pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR`. The container harness pulls `odoo:19.0` and starts it against `postgres:15-alpine` (already verified in Wave 0 Task 2 smoke, but the full suite run is needed to confirm all 19 tests pass).
  </what-built>
  <how-to-verify>
    Run: `ODOO_VERSION=19.0 uv run pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR`

    Scenario A — All 19 tests pass:
    Type "go: 19.0 green, N passed" — proceed to Plan 06 (sample-PR verification).

    Scenario B — Some tests fail (schema delta / assertion mismatch):
    Apply the same D-08 fix protocol as Task 1 Step 2. Fix in-phase, re-run, confirm green, then type "go: 19.0 green after fixes, N passed".

    Scenario C — Container startup failure (Postgres/Odoo version incompatibility):
    This triggers D-09/D-10 halt protocol:
    1. Capture the Odoo container logs from the pytest output.
    2. Look for Postgres version incompatibility messages (e.g., "requires PostgreSQL 16+").
    3. File an issue on https://github.com/godoo-dev/godoo-py with: reproduction steps, container log excerpt, the RESEARCH.md §Research Finding 10 analysis.
    4. Type "blocked: 19.0 container startup failure — D-09 protocol invoked, issue filed at [URL]"
    5. Phase 03.1 is marked BLOCKED. Do NOT proceed to Plan 06. The phase resumes only after an upstream `godoo-testcontainers` fix is published.

    Scenario D — `odoo:19.0` image pull failure (image does not exist):
    Already guarded in Wave 0; should not occur here. If it does, same halt procedure as Scenario C.
  </how-to-verify>
  <resume-signal>
    Type "go: 19.0 green, N passed" OR "go: 19.0 green after fixes, N passed" OR "blocked: [reason and upstream issue URL]"
  </resume-signal>
  <acceptance_criteria>
    If "go": `ODOO_VERSION=19.0 uv run pytest tests/acceptance -m integration -q` exits 0 with same test count as 17.0/18.0. Zero skipif/xfail markers.
    If "blocked": D-09 protocol executed: upstream issue filed with reproduction steps, phase BLOCKED status recorded in STATE.md.
  </acceptance_criteria>
  <done>
    All three versions (17.0, 18.0, 19.0) pass 19 integration tests locally, OR the phase is formally blocked with upstream issue filed. No version passes with fewer tests than 17.0 baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Test execution only — no production surface changes unless shake-out fixes require test or code edits.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-06 | Tampering | Shake-out fixes could introduce skipif/xfail markers (D-08 violation) | mitigate | Acceptance criteria explicitly guards: `grep -rn "skipif\|xfail" tests/acceptance/` must return 0. |
| T-03.1-07 | Denial of Service | Upstream godoo-py bug masks as stateman test failure | mitigate | D-09 protocol: identify root cause before patching; halt and file upstream if godoo-py is implicated. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs expected; if shake-out requires a godoo-testcontainers bump, commit lockfile per lockfile-discipline rule. |
</threat_model>

<verification>
Local matrix baseline (run sequentially before declaring done):
1. `ODOO_VERSION=17.0 uv run pytest tests/acceptance -m integration -q` → 19 passed
2. `ODOO_VERSION=18.0 uv run pytest tests/acceptance -m integration -q` → 19 passed
3. `ODOO_VERSION=19.0 uv run pytest tests/acceptance -m integration -q` → 19 passed (or phase blocked)

No new skip markers:
`grep -rn "skipif\|xfail" tests/acceptance/` → 0 matches

Lint clean:
`uv run ruff check tests/acceptance/ src/godoo_stateman/` → exits 0
</verification>

<success_criteria>
1. 17.0, 18.0, 19.0 all produce "N passed" with the same N (no version produces fewer collected tests).
2. Zero skipif/xfail markers introduced.
3. If 19.0 fails with a container startup error: upstream issue filed, phase blocked, evidence documented.
4. If 19.0 fails with a test assertion error: fixed in-phase, no skip markers, re-run confirms green.
</success_criteria>

<output>
Create `.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-05-SUMMARY.md` when done.
</output>
