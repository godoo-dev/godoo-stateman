---
phase: "03.1"
plan: 6
type: execute
wave: 5
depends_on:
  - "03.1-05"
files_modified: []
autonomous: false
requirements:
  - VAL-03

must_haves:
  truths:
    - "A sample PR exists on GitHub targeting the develop branch"
    - "All three CI matrix entries (Integration Tests Odoo 17.0, 18.0, 19.0) show green checkmarks"
    - "Each CI job collected a non-zero test count (≥ 19) — no false-positive empty-collection run"
    - "The CI PR run URL is recorded in the verification report"
  artifacts:
    - path: ".planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VERIFICATION.md"
      provides: "Phase verification report with CI run URL and all three matrix entries confirmed green"
      contains: "github.com"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "GitHub Actions integration matrix"
      via: "push/PR trigger"
      pattern: "Integration Tests \\(Odoo"
---

<objective>
Push all Phase 03.1 changes to GitHub, open a draft PR, and confirm all three CI matrix entries pass green with non-zero test counts. This is the mandatory proof-of-concept for VAL-03 SC-2.

Purpose: Local runs confirm correctness for the developer's Docker environment, but CI is the canonical gate — it runs the matrix in isolation with the exact same Ubuntu + uv + Docker setup that production CI will use. Only a green CI run proves SC-2.

Output: A linked GitHub Actions run URL showing `Integration Tests (Odoo 17.0)`, `Integration Tests (Odoo 18.0)`, and `Integration Tests (Odoo 19.0)` all passing, plus a written `03.1-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md
@.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-05-SUMMARY.md

<interfaces>
<!-- CI workflow structure from RESEARCH.md §2 -->

The CI file is .github/workflows/test.yml (NOT ci.yml — D-05).
The integration job name is "integration" with matrix key "odoo_version" (underscore).
Job display names in GitHub Actions: "Integration Tests (Odoo 17.0)", "Integration Tests (Odoo 18.0)", "Integration Tests (Odoo 19.0)".
Integration test command: `uv run --no-sources pytest tests/acceptance -v -s -m integration --log-cli-level=ERROR`

False-positive detection (VALIDATION.md):
  A matrix entry that collected 0 tests is NOT green.
  pytest exits 5 on 0-collection, which CI treats as non-zero → job failure.
  Expected count: ≥ 19 integration tests per entry.
  Parity check: all three entries should collect the same N.

The PR does NOT need to be merged — only that all three CI jobs pass on the branch is required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push branch and open draft PR</name>
  <files></files>
  <read_first>
    - .github/workflows/test.yml — confirm the integration job exists with the matrix as expected; no changes needed, just read to verify the trigger conditions (push / pull_request)
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-05-SUMMARY.md — confirm all local shake-out results were green before pushing
  </read_first>
  <action>
    Step 1 — Ensure all Phase 03.1 changes are committed on the `develop` branch. Run `git status` to confirm clean working tree. If there are unstaged changes from shake-out fixes, stage and commit them with conventional commit message `fix(03.1): shake-out fixes for Odoo 18/19 compatibility` before pushing.

    Step 2 — Push the develop branch to GitHub: `git push origin develop`.

    Step 3 — The integration job in `test.yml` triggers on `push` to `develop` OR on `pull_request` targeting `develop`. Check whether a push alone triggers the integration matrix (it does if `test.yml` has `on: push: branches: [develop]`). If the matrix only triggers on PR, open a draft PR:
    `gh pr create --draft --base develop --title "feat(03.1): multi-Odoo-version CI test matrix" --body "Phase 03.1 closes the OdooVersion hardcode gap and validates the acceptance suite on Odoo 17.0, 18.0, 19.0. See .planning/phases/03.1-multi-odoo-version-ci-test-matrix/ for context."`

    Step 4 — Report the PR URL (or the run URL if push-triggered) so the user can monitor.

    Note: Do NOT merge the PR yet — Plan 06 only requires confirmation that CI is green, not that the PR is merged.
  </action>
  <verify>
    <automated>git status 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    1. `git status` shows clean working tree (nothing to commit).
    2. `git push origin develop` succeeds (or already up-to-date).
    3. Either a push-triggered GitHub Actions run is in progress, OR a draft PR is open and CI has started. URL is available.
  </acceptance_criteria>
  <done>
    All Phase 03.1 commits are on `develop` (or a draft PR branch). CI run is in progress on GitHub Actions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm all three CI matrix entries pass green</name>
  <read_first>
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VALIDATION.md — Manual-Only Verifications table (first row) + False-Positive Guards section
    - .planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-CONTEXT.md — D-08 (no skipif/xfail) + SC-2 success criterion
  </read_first>
  <what-built>
    All Phase 03.1 code changes are pushed to GitHub. The `test.yml` matrix is running (or has already run) with `ODOO_VERSION` set to `17.0`, `18.0`, and `19.0` in three parallel jobs.
  </what-built>
  <how-to-verify>
    1. Open the GitHub Actions run for the commit/PR (use the URL from Task 1 output, or navigate to https://github.com/godoo-dev/godoo-stateman/actions).
    2. Find the run for the Phase 03.1 commit/branch.
    3. Verify the following jobs are all green (✓ checkmark):
       - `Integration Tests (Odoo 17.0)` — must show "N passed" in the test output, where N ≥ 19.
       - `Integration Tests (Odoo 18.0)` — must show same N passed.
       - `Integration Tests (Odoo 19.0)` — must show same N passed.
    4. False-positive check: Click into each integration job's test output. Confirm the line reads "N passed" NOT "0 passed, N deselected". If any job shows 0 collected, that is a false positive — the `-m integration` marker filter failed — investigate conftest.py marker registration.
    5. Also verify: `unit-tests` and `lint` jobs are green (these are not matrix jobs; they should have been green since before Phase 03.1).

    If all three matrix jobs are green:
    Type "verified: all 3 matrix green — [CI run URL] — 17.0: N passed, 18.0: N passed, 19.0: N passed"

    If one or more matrix jobs fail:
    Capture the failure summary from the CI log. Return to the code, diagnose the failure, fix it (no skipif/xfail per D-08), re-push, wait for the new CI run. Repeat until all three are green.
    Type "fixing: [failure summary]" to indicate you are iterating.
  </how-to-verify>
  <resume-signal>
    Type "verified: all 3 matrix green — [CI run URL] — 17.0: N passed, 18.0: N passed, 19.0: N passed" once all three are confirmed green with non-zero test counts.
  </resume-signal>
  <acceptance_criteria>
    1. Three CI jobs all show ✓ green.
    2. Each job's pytest output shows ≥ 19 collected and 0 failures.
    3. All three jobs report the same test count (parity).
    4. CI run URL is recorded in 03.1-VERIFICATION.md.
    5. The `unit-tests` and `lint` jobs are also green (not regressed by Phase 03.1 changes).
  </acceptance_criteria>
  <done>
    All three matrix entries (17.0, 18.0, 19.0) are green on GitHub Actions with non-zero test counts. The CI run URL is recorded. Phase 03.1 VAL-03 SC-2 is satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| develop branch → GitHub Actions | Code execution on GitHub-managed runners; no secrets involved beyond GITHUB_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-08 | Tampering | CI workflow injection if test.yml were modified in this phase | accept | This plan makes no changes to test.yml; the workflow is read-only here. If a prior plan accidentally modified test.yml, verify via `git diff HEAD~1 -- .github/workflows/test.yml` before pushing. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan |
</threat_model>

<verification>
This plan's verification IS the CI run. No additional automated check is possible beyond observing the GitHub Actions run.

Pre-push unit smoke (must pass before push):
`uv run pytest -m "not integration" -q` — exits 0

Post-CI evidence checklist:
- [ ] GitHub Actions run URL recorded in 03.1-VERIFICATION.md
- [ ] "Integration Tests (Odoo 17.0)" — ✓ green, ≥ 19 collected
- [ ] "Integration Tests (Odoo 18.0)" — ✓ green, ≥ 19 collected
- [ ] "Integration Tests (Odoo 19.0)" — ✓ green, ≥ 19 collected
- [ ] "unit-tests" — ✓ green (not regressed)
- [ ] "lint" — ✓ green (not regressed)
</verification>

<success_criteria>
1. All three `Integration Tests (Odoo N.N)` CI jobs green with ≥ 19 tests collected.
2. No CI job shows 0 collected tests (false-positive guard).
3. No skipif/xfail markers in the acceptance test files.
4. `03.1-VERIFICATION.md` contains the CI run URL and per-version pass counts.
5. Phase 03.1 complete: VAL-03 is satisfied.
</success_criteria>

<output>
Create `.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-06-SUMMARY.md` when done.
Also create `.planning/phases/03.1-multi-odoo-version-ci-test-matrix/03.1-VERIFICATION.md` with the CI run evidence.
</output>
