# Phase 03.1: Multi-Odoo-version CI test matrix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-28
**Phase:** 03.1-multi-odoo-version-ci-test-matrix
**Areas discussed:** OdooVersion(17, 0) hardcode strategy, SC re-alignment + VAL-03 collision, Failure policy on 18/19 during stabilization, Postgres pairing per Odoo version

---

## OdooVersion(17, 0) hardcode strategy

### Q1: Where should `ODOO_VERSION` env be parsed into `OdooVersion`?

| Option | Description | Selected |
|--------|-------------|----------|
| pytest fixture in acceptance conftest | Session-scoped `odoo_version: OdooVersion` fixture next to the existing `odoo` fixture. test_plan_import.py and test_snapshot.py inject it; assertions use it directly. plan.py fixed separately. | ✓ |
| Read os.environ at every call site | Replicate snapshot.py:61 pattern at every consumer. No new abstraction but env-reading scattered across src/ and tests/. | |
| New helper in src + import in tests | Extract env→OdooVersion parsing into `src/godoo_stateman/version.py`. Centralized but adds a new module. | |

**User's choice:** pytest fixture in acceptance conftest (Recommended)
**Notes:** Drives D-01, D-02, D-03 in CONTEXT.md.

### Q2: How should `test_snapshot.py`'s literal `== "17.0"` assertions be rewritten?

| Option | Description | Selected |
|--------|-------------|----------|
| Assert against the odoo_version fixture | `assert schema.odoo_version == str(odoo_version)`. Works for all matrix entries; clear failure messages. | ✓ |
| Assert any valid version string | Regex match `\d+\.\d+`. Loses the round-trip check. | |
| Parametrize the snapshot tests | @pytest.mark.parametrize over the three versions inside the test. Redundant with CI matrix (×3 ×3 = ×9). | |

**User's choice:** Assert against the odoo_version fixture (Recommended)
**Notes:** Drives D-03 in CONTEXT.md.

### Q3: Should `plan.py:90` also use the same env-reading helper as `snapshot.py:61`?

| Option | Description | Selected |
|--------|-------------|----------|
| Read ODOO_VERSION env, same as snapshot.py | Symmetric with snapshot command. Inline-copy the 5 lines from snapshot.py. | ✓ |
| Add an explicit --odoo-version CLI flag | More explicit for operators but CLI surface change, out of scope. | |
| Leave plan.py hardcoded; only fix the tests | Strictly minimal but ships a known bug. | |

**User's choice:** Read ODOO_VERSION env, same as snapshot.py (Recommended)
**Notes:** Drives D-04 in CONTEXT.md. Helper extraction explicitly deferred.

---

## SC re-alignment + VAL-03 collision

### Q1: Workflow file name: `test.yml` (already exists) vs `ci.yml` (what ROADMAP SC-1 says)?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep test.yml + amend ROADMAP SC-1 wording | Matches godoo-py and was shipped 2026-05-23. Rename is git-history churn for no gain. | ✓ |
| Rename test.yml → ci.yml | Match SC-1 literally. Breaks symmetry with godoo-py, no functional benefit. | |

**User's choice:** Keep test.yml + amend ROADMAP SC-1 wording (Recommended)
**Notes:** Drives D-05 in CONTEXT.md.

### Q2: SC-2 says "18 integration tests" but actual count is 19. Fix?

| Option | Description | Selected |
|--------|-------------|----------|
| Amend SC-2 to drop the literal count | Rewrite to "all @pytest.mark.integration tests pass". Future-proof. | ✓ |
| Amend SC-2 to say 19 | Goes stale next time a test is added. | |

**User's choice:** Amend SC-2 to drop the literal count (Recommended)
**Notes:** Drives D-06 in CONTEXT.md.

### Q3: VAL-03 maps to "TS LOC benchmark / Phase 5" but Phase 03.1 also cites it. Resolution?

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite VAL-03 = multi-version, move LOC benchmark to VAL-04 | Multi-version validation is the substantive requirement; LOC benchmark is parity check. | ✓ |
| Keep VAL-03 as LOC, add new VAL-04 for multi-version | Symmetric inverse; same end-state. | |
| Leave dual meaning, document in REQUIREMENTS.md | Cheapest but muddies the traceability table. | |

**User's choice:** Rewrite VAL-03 = multi-version, move LOC benchmark to VAL-04 (Recommended)
**Notes:** Drives D-07 in CONTEXT.md.

---

## Failure policy on 18/19 during stabilization

### Q1: If a real Odoo 18/19 test fails, how should the phase handle it?

| Option | Description | Selected |
|--------|-------------|----------|
| Required-to-pass from day 1, fix shake-out in-phase | Phase done only when all three matrix entries are green. No skipif/xfail. | ✓ |
| continue-on-error: true for 18/19, follow-up phase 03.2 to lift | Matrix exists but 18/19 informational. Risk: 03.2 never happens. | |
| Scope phase to matrix-only; spawn 03.2 for shake-out fixes upfront | Plan two phases now. Clearer but adds a phase. | |

**User's choice:** Required-to-pass from day 1, fix shake-out in-phase (Recommended)
**Notes:** Drives D-08 in CONTEXT.md.

### Q2: If shake-out reveals godoo-client / godoo-introspection bugs, what's the policy?

| Option | Description | Selected |
|--------|-------------|----------|
| File upstream issue + pin a working godoo-py version | Raise the bug upstream, pin a known-good version, note TODO. | |
| Workaround locally in stateman | Faster unblock; technical debt because stateman owns workaround. | |
| Block phase until upstream fixes | Pause Phase 03.1 until godoo-py ships a fix. Slowest. | ✓ |

**User's choice:** Block phase until upstream fixes
**Notes:** Drives D-09 in CONTEXT.md. Stronger upstream-first stance than the recommended option — stateman should not own workarounds for godoo-py / godoo-testcontainers bugs.

---

## Postgres pairing per Odoo version

### Q1: Odoo 19 may require PG ≥ 16; testcontainer default appears to be postgres:15-alpine. Strategy?

| Option | Description | Selected |
|--------|-------------|----------|
| Investigate godoo-testcontainers source first, then decide | Read container.py to see how Postgres image is selected before adding plumbing. | |
| Pin Postgres per matrix entry now | Add per-matrix postgres-version and thread through TestHarness. Defensive. | |
| Accept testcontainer defaults, patch only if Odoo 19 fails | Minimum upfront work; possible CI red until discovered. | |
| **Other (user-typed):** Accept defaults; if Odoo 19 fails, halt and upgrade `godoo-testcontainers` defaults upstream | Aligns with D-09 (block on upstream bugs). Don't add stateman-local PG plumbing — fix the harness instead. | ✓ |

**User's choice:** Other — accept defaults; halt and upgrade godoo-testcontainers upstream if Odoo 19 fails
**Notes:** Drives D-10 in CONTEXT.md. Consistent with the upstream-first policy from D-09.

---

## Claude's Discretion

- Helper-module extraction for `ODOO_VERSION` env parsing — deferred; inline duplication acceptable.
- Exact regex used in fixture and plan.py — match `snapshot.py:61` verbatim.
- Fixture file location (`tests/conftest.py` vs `tests/acceptance/conftest.py`) — planner picks based on existing structure.
- Ordering of work — planner sequences within the dependency chain (hardcode fix → matrix exercise → shake-out fixes → doc updates).

## Deferred Ideas

- Extract `ODOO_VERSION`-env parsing into a shared `src/godoo_stateman/version.py` module — revisit when a third live consumer appears.
- Add explicit `--odoo-version` CLI flag to `plan` command — out of phase scope (CLI surface change).
- Per-Odoo-version schema-snapshot files on disk (`tests/snapshots/{17,18,19}/`) — only needed if tests start round-tripping through disk.
- Per-matrix-entry Postgres pinning in stateman's `test.yml` — only if upstream fix in `godoo-testcontainers` proves infeasible.
- Renaming `test.yml` → `ci.yml` — rejected this phase; revisit only if godoo-py renames first.
