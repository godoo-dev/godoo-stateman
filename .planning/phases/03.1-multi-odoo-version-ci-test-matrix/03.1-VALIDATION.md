---
phase: 03.1
slug: multi-odoo-version-ci-test-matrix
status: passed
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-28
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio |
| **Config file** | pyproject.toml (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest -m "not integration" -q` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | unit suite ~5 s; integration suite ~3–5 min per Odoo version (CI matrix runs three in parallel) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest -m "not integration" -q` (unit + lint smoke).
- **After every plan wave:** Run `uv run pytest -q` (full local suite, single Odoo version = 17.0).
- **Before `/gsd:verify-work`:** Full local suite green AND a sample PR shows all 3 matrix entries green on GitHub Actions.
- **Max feedback latency:** unit ~10 s; local full ~5 min; CI matrix ~10 min wall-clock per PR.

---

## Per-Task Verification Map

> Populated by the planner — one row per task. Tasks that only edit docs / CI / requirements use `manual-only` or `lint+typecheck` rows.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 1 | VAL-03 | — | n/a | unit | `uv run pytest tests/acceptance/test_plan_import.py -q` | ✅ | ⬜ pending |
| 03.1-NN-NN | NN | N | VAL-03 | — | n/a | integration | `uv run pytest -m integration -q` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Pre-flight: `docker manifest inspect odoo:19.0` returns success (otherwise halt phase per D-10 + D-09). — VERIFIED 2026-05-28: odoo:17.0, odoo:18.0, odoo:19.0 all return `schemaVersion: 2` from Docker Hub.
- [x] Pre-flight: `docker pull odoo:19.0 && docker pull odoo:18.0` succeed locally. — VERIFIED 2026-05-28: images pulled implicitly during smoke test (odoo:19.0 pulled as part of testcontainer startup).
- [x] Smoke: `ODOO_VERSION=19.0 uv run pytest tests/acceptance/test_snapshot.py::test_schema_registry_get -v -s --log-cli-level=ERROR` boots a 19.0 testcontainer against `postgres:15-alpine` without container-startup failure (proves D-10 holds before mass shake-out). — VERIFIED 2026-05-28: 1 PASSED in 230.08s; no TimeoutError, no PG init error. D-10 hold NOT triggered.
- [ ] `tests/conftest.py` exposes the new session-scoped `odoo_version` fixture (D-01) before any acceptance test is rewritten.

*If any Wave 0 item fails: halt phase per D-09 — file upstream issue/PR against `godoo-testcontainers`, pin known-good, then resume.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| All 3 matrix entries green on a real GitHub Actions PR | VAL-03 (SC-2) | Requires a live PR run; cannot be asserted locally | 1) Push branch to GitHub. 2) Open a draft PR. 3) Wait for `test.yml` workflow to complete. 4) Confirm `test (17.0)`, `test (18.0)`, `test (19.0)` jobs all show ✓ with non-zero collected test counts (guard against false-positive "0 collected, 0 failed"). 5) Link the PR run URL in the verification report. |
| `PROJECT.md` and `CLAUDE.md` doc-string substitution (D-11) | VAL-03 (SC-4) | Wording change — review by author | Diff-check: `git diff PROJECT.md CLAUDE.md` shows the single canonical replacement string from RESEARCH §6, no other prose changes. |
| REQUIREMENTS.md VAL-03 → VAL-04 split (D-07) | VAL-03 (SC-4 spirit) | Requirements file is human-curated | Diff-check: VAL-03 now reads "multi-Odoo-version validation: acceptance suite passes on 17.0, 18.0, 19.0 in CI matrix"; VAL-04 carries the prior "TS benchmark 29 files / 4,564 LOC" semantic assigned to Phase 5; traceability table row updated. |

---

## False-Positive Guards

These checks catch the "matrix green but tests didn't actually run" failure mode (called out in RESEARCH §Validation Architecture):

- [x] Each CI matrix job's pytest output reports a non-zero `collected` count — confirmed 18 per entry in CI run 26579066945 (2026-05-28).
- [x] `pytest -m integration -q` summary line shows the same N tests on all three matrix entries (parity, not just "all green") — 18 = 18 = 18.
- [x] `ODOO_VERSION` env-var injection is asserted in at least one test (e.g., `odoo_version` fixture parses to the matrix value), so a missing/empty env collapses to the 17.0 default and is caught by a 17.0-vs-18.0 mismatch assertion.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (Odoo 19.0 image availability, 19.0 startup smoke)
- [x] No watch-mode flags
- [x] Feedback latency < 600 s for CI matrix (integration jobs completed in 66-80s each)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** PASSED — 2026-05-28 (CI run 26579066945: all 3 matrix entries green, 18 tests each)
