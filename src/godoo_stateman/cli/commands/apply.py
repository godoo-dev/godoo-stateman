"""[stub] apply command — execute a plan against live Odoo (Phase 4)."""

from __future__ import annotations

import typer
from rich.console import Console


def apply() -> None:
    """[stub] Execute a plan against live Odoo (Phase 4)."""
    console = Console()
    console.print("[yellow]apply: not yet implemented (Phase 4)[/yellow]")
    raise typer.Exit(code=1)
