"""[stub] import command — import existing Odoo records into stateman state (Phase 3)."""

from __future__ import annotations

import typer
from rich.console import Console


def import_() -> None:
    """[stub] Import existing Odoo records into stateman state (Phase 3)."""
    console = Console()
    console.print("[yellow]import: not yet implemented (Phase 3)[/yellow]")
    raise typer.Exit(code=1)
