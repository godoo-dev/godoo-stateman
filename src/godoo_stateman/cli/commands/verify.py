"""[stub] verify command — check live Odoo matches desired state (Phase 5)."""

from __future__ import annotations

import typer
from rich.console import Console


def verify() -> None:
    """[stub] Check live Odoo matches desired state (Phase 5)."""
    console = Console()
    console.print("[yellow]verify: not yet implemented (Phase 5)[/yellow]")
    raise typer.Exit(code=1)
